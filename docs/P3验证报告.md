# P3 预算结算 · 独立验证报告（后端）

> 验证人：QA 严过关（software-qa-engineer-3，fresh eyes 独立复现，不复述工程师自报 9 步冒烟）。
> 验证对象：`origin/develop = 73ef3ce`（4 批：`b02bdf6`/`c03ad1e`/`b471932`/`73ef3ce`）。
> 方法：JDK17(`ms-17.0.18`) + `backend/` Gradle 8.7 wrapper `clean build`；真实 MySQL8@3307（先手工执行 `scripts/sql/p3_schema.sql`）+ Redis7@6380；后端运行态（`:18080`）HTTP API 实测 + 直查 DB 对账；**并发用例为 QA 自写**（Python 线程 barrier，自建 `QA_P3*` 干净链路 + 预算线 990110/990111/990112）。
> 约束：未改业务源码、未动飞书、未 push。

---

## 0. 结论

**IS_PASS = false（P1 级 1 项 + P2 级 5 项；★预算事务并发与守恒合规重点全部通过）**

- **★预算占用/释放/核销事务方案（§3）核心正确**：同月并发恰一方占用、跨月不互阻、全库 `Σ(occupy_log 带符号) == used_amount` 零差异（多轮复检）、先占后审零占用 + BUDGET 升级 + 超支放行、申请驳回释放、订单取消释放、月度锁——全部独立复现通过。
- **#35【P1】**：订单变更**增量**（加量）占用因 `setDeptId(null)` 永远 4000，变更加量路径完全不可用。
- P2×5：#36 申请作废端点缺失（假占用无法释放）、#39 付款累计校验可被超额绕过、#42 服务扣款与尾款校验矛盾、#45 比价未接价格库、#46 无 final 结算的付清订单永不转 PAID。
- 1 项口径偏离（#37 转移实现）需架构裁决。

### 0.1 构建与回归

| 项 | 结果 |
|---|---|
| `clean build`（JDK17 + 8.7 wrapper） | ✅ **74 例全绿**（P1 29 + P2 29 + P3 16：`BudgetOccupy*Test` 并发 6 / `SettlementPaymentTest` 5 / `PriceHistoryTest` 4） |
| 前端（未改动，抽验） | 上轮 386.76 kB 无告警结论维持 |
| P1+P2 回归抽样 | ✅ 两级审批留痕 / 资质闭环 / 上传缺件 4000 / 导出 PK / 畸形 JSON 4000 / 401 / 404 / 菜单（P2 13 项 + P3 预算 4 项 501-504） |

---

## 1. ★ 预算占用/释放/核销并发与事务（§3，独立复现）

QA 自建预算：`budget_header 990100(dept1/2026)` + 线 `990110`(10月 amount 10000)、`990111`(11月 500)、`990112`(12月 1056)。SKU BOX rate=12，占用口径 = `qty × priceEstimate`。

| # | 规则（规格 §5） | 实测 | 判定 |
|---|---|---|---|
| 行1 | 余额足：提交即占用 | A1(2 BOX=176)：`used 0→176`、log(OCCUPY,176,balance 0→176)、审批后占用保留 | ✅ |
| 行2 | 超预算：**零占用** + `BUDGET_PENDING` + `budget_status=2` + BUDGET 升级审批 | A2(6 BOX=528>500)：status=1、budget_status=2、**log 0 条、used 不变**、BUDGET 任务=1（payload 含明细） | ✅ |
| 行3 | BUDGET 通过 → 超支占用生效 | approve 后 used 0→528（执行率 105%）、→ PURCHASE_PENDING → APPROVED | ✅ |
| 行4 | BUDGET 驳回 → 回 DRAFT + 释放 | reject 后 **used 不变**（本就零占用）；状态见 #40 备注（REJECTED(4) 可重提再审，非字面回 DRAFT(0)，功能等价） | ✅* |
| 行5 | 申请驳回 → 释放（含负向流水） | A4(3 BOX=264)：`used −264`、log(action=RELEASE,**−264**,balance 链正确) | ✅ |
| 行6 | 订单生成"占用主体转移"（申请减、订单增，不重复计） | 实现=**申请侧释放 −176 + 订单侧全额占用 2112（转移 176+差额 1936）**：链式守恒 ✅ 但 used 净增 +1936 ≠ 规格字面"used 不变" → **#37 口径偏离待裁决**；`purchase_order.budget_occupied` **实写**（承接清单#2 ✅）；申请 → PARTIAL_ORDER | ✅*/⚠️ |
| 行7/8 | 变更对冲 | 减量(newQty 1)：`used −1056`、log RELEASE ✅；**增量(newQty 2/3) → 4000「预算占用命令非法」= #35【P1】** | 减✅/增❌ |
| 行9 | 订单取消 → 释放本单占用 | O2(2112) 取消：`used −2112`、RELEASE 流水在案、合同 available 回冲 | ✅ |
| 行10 | 部分转单后作废申请 → 释放余量 | 部分转单 PARTIAL_ORDER ✅；**作废/关闭端点不存在**（`/close` `/void` `/cancel` 均 404，`closeApply` 未实现）= **#36【P2】** | ⚠️ |
| 行13a | 同月并发恰一方成功 | 12月线(1056) 两申请各 616 并发 submit：**恰一方占用(budget_status=1)另一方拦截升级(=2)**、used=616 | ✅ |
| 行13b | 不同月份并发不互阻 | 10月/11月/12月三线跨月并发提交：全部立即裁决、无死锁（<2s） | ✅ |
| 守恒 | `Σ(log 带符号) == used_amount` 全库零差异 | 多轮复检（占用/释放/核销/转移/调整混合后）：**990110=12112、990111=704、990112=616 全部零差异** | ✅ |

## 2. ★ 主流程 E2E（申请→拦截→升级→超支→驳回→下单→取消→结算→付款→对账）

QA 自建链路（不复用工程师冒烟）：申请占用 → 超预算拦截+BUDGET 升级 → 超支放行 → 驳回释放 → 下单转移 → 变更对冲 → 取消释放 → 到货入库 → 结算核销 → 付款两阶段 → 订单 PAID。全程状态机与 DB 对账一致；守恒终检零差异。

## 3. 结算细节（T05/T06）

- **双入口** ✅：`draft/from-order` 与 `draft/from-arrival`（优先 `qty_stored`）均返回建议草稿。
- **创建校验** ✅：结算数量 > 可结余量 → 3000「结算数量超出可结余量：入库 12，已结 12」；到货粒度防重（同一 arrival 重复创建被拒）；尾款 `isFinal=1` 金额 ≠ 累计+本次 → 3000（实测 500/422.4 错额均拒、1584 差额通过）。
- **阶段结算** ✅：O4 双阶段 50%/50%（528+1584=2112）→ 尾款审批后订单 → SETTLED(3)。
- **核销** ✅：结算审批后 `used_amount` 不变、log(action=WRITE_OFF, 633.60) 独立记录（构成转移；⚠️ 规格字面"负向"，见 #38 P3）。
- **服务结算**：amount 自动扣减考核扣款 ✅（1056−100=956），但 **`deduct_amount` 列未回填=0**（#43【P2】）；且 **isFinal=1 校验要求=应结 1056 与扣款 956 矛盾** → 服务链无法走 final（#42【P2】）。
- **驳回留痕可重提** ✅：结算驳回 → 重提 → 再审批通过。

## 4. 付款（T07）

- 仅可对审批通过结算发起 ✅；两阶段（创建→SETTLEMENT 审批→confirm 凭证+日期）✅。
- **累计 ≤ 结算额**：PAID 口径校验生效（>结算额 999 拒）✅；但**仅计 status=PAID 付款** → 300(已付)+600(在途) = 900 > 528 落库成功 = **#39【P2】超额窗口**。
- **订单 PAID 正向** ✅：O4（有 isFinal 结算）全额付清 → **PAID(5)**（`settleOrderIfFullyPaid` 触发）。
- **#46【P2】**：无 isFinal 结算的订单（如服务链，见 #42）付清后停 RECEIVED(2)，guard `status==SETTLED` 永不满足 → 永不 PAID。
- 对账单 ✅：`/payments/statement`（totalPayable/totalPaid/balance + 逐笔 rows）+ `/export` OOXML（PK 魔数，3917B）。

## 5. price_history（T08）

- **三埋点** ✅：QUOTATION（报价采纳 accept）/ AWARD（定标审批通过）/ ORDER（订单生成）均有落库。
- **异常价** ✅：偏离均价 50%（132 vs 88）→ `audit_status=PENDING` → `/price-histories/pending` 可见 → audit 通过 → APPROVED。
- **比价切换** ❌ **#45【P2】**：`InquiryServiceImpl.comparison()` 仍纯实时计算（quotation 表），未接入价格库/空库兜底逻辑——任务点 6"历史切价格库"未实现（无回归风险，功能缺失）。

## 6. 台账 + 月度调整（T04/T09）

- 执行台账 `GET /budgets/execution` + 流水 `/logs?lineId=` ✅。
- **调减校验** ✅：`newAmount=100 < used 616` → 3000「调减后额度 100 不能低于已占用 616.00」。
- **超 20% 阈值** ✅：+23%/−46% 调整 → 走 BUDGET 审批（bizId=lineId），审批通过生效；**免审** ✅：+7.7% 直接生效。
- 校准工具 ✅：`/{lineId}/recalibrate`；校准/调整均写 `budget_occupy_log`（守恒口径内）。

## 7. 延后项 / 契约核查（对照台账）

| 项 | 结果 |
|---|---|
| D3（预算硬控）/D8（考核扣款取数） | ✅ 代码标注更新为「D3 已落地 P3」「D8 已落地 P3」（`PurchaseApplyServiceImpl.java:47/146/359`、`SettlementSaveReqVO.java:15` 等） |
| `used_amount` 真实写入 | ✅（P2 软校验→P3 硬控升级，全链路实测） |
| 新增枚举 @JsonValue | ✅ **零注解**（初判命中系注释文字"不加 @JsonValue"的 grep 假阳性，已复核） |
| 枚举 name 契约（两收一吐） | ✅ 吐 name（`ONE_TIME/SERVICE/SETTLED/AWARD/APPROVED`）；收 name + 数值兜底 + 非法 4000（`MATERIAL`/`0` 均 code=0 到业务层；`NOT_REAL`→4000）。P3 备注：非法值报文为通用「请求体格式非法」而非设计的友好枚举列表提示 |
| `IBudgetSoftCheckService` 契约不变 | ✅（26 行接口未动） |
| P3 菜单种子 | ✅ 501-504（科目/项目/导入/台账）；结算/付款/成本无二级菜单（前端下轮，符合批次规划） |

## 8. 回归（P1+P2 抽样）

✅ 两级审批留痕（PURCHASE_APPLY DEPT_HEAD→PURCHASE_DEPT）；资质 review→callback 闭环；上传缺 file → 4000；申请导出 OOXML（PK）；畸形 JSON → 4000；未登录 401；未知路径 404 不回退；菜单 P2 13 项 + P3 4 项。P2 三重校验（合同/订单维度）并发由第 6 轮（`9d8ec40`）维持，本轮以预算维度并发覆盖。

## 9. 问题清单（分级，带证据）

### P0
- 无。

### P1（须修）
35. **【后端·P1】订单变更"增量"占用永远 4000「预算占用命令非法」** — `OrderServiceImpl.java:434` 变更加量分支构造 `BudgetOccupyCmd` 时 `setDeptId(null)`（未回填申请的 dept/subject/period）→ `BudgetOccupyServiceImpl.java:511` 合法性校验拒绝。复现：对已有订单 `POST /api/v1/orders/{id}/change` `{"items":[{"orderItemId":…,"newQty":+1}]}` → 4000。**变更减量释放正常，加量路径完全不可用**。

### P2（建议修，阻断部分链路终态）
36. **申请作废/关闭端点缺失** — 设计 §2 行10 `closeApply(void)` 未实现（`PurchaseApplyController` 无 /close /void /cancel；service 无 `closeApply`）。APPROVED 未转单申请的占用**永久假占用**，只能走驳回路径（但已 APPROVED 不可驳回）。`PurchaseApplyController.java` 全量端点为证。
39. **付款累计校验仅计 PAID** — `PaymentServiceImpl` `sumPaidAmount` 过滤 status=PAID：在途（待审/待确认）付款不计入 → 可超额付款落库（实测 S1(528) 实付 300+600=900）。建议改为计入非 REJECTED 付款。
42. **服务扣款与尾款校验矛盾** — 服务结算 `isFinal=1` 校验要求 amount=订单应结 1056，但服务结算金额自动扣减考核扣款（956）→ 拒绝；只能 `isFinal=0` → 配合 #46 服务订单**无法到达 SETTLED/PAID 终态**。
43. **服务结算 `deduct_amount` 未回填** — 实测 settlement 行 `amount=956.00, deduct_amount=0.00`：金额已扣但审计列空，台账/审计不可追溯。
45. **比价未接价格库** — `InquiryServiceImpl.comparison()` 纯实时计算；price_history 仅埋点 + `/recent` 查询。设计任务点 6「历史切价格库 + 空库兜底」未实现。
46. **付清订单无 final 结算 → 永不 PAID** — `PaymentServiceImpl.settleOrderIfFullyPaid` guard `order.status==SETTLED`：无 isFinal 结算的订单付清后停 RECEIVED(2)（O3/svc 单实测）。与 #42 叠加使服务链路无终态。

### P3（口径/提示）
37. **转移口径偏离（需架构裁决）** — 订单生成=申请释放+订单全额占用：守恒成立但 used 净增差额（≠规格行6 字面"used 不变"），台账执行率随之抬升。
38. **核销流水方向字面** — log action=WRITE_OFF 存正金额（规格 AC① 写"负向"）；语义自洽，建议改规格描述。
40. **BUDGET 驳回后状态** — REJECTED(4) 停留（规格行4 字面"回 DRAFT"），可重提再审，功能等价；建议规格注明。
- 非法枚举报文为通用「请求体格式非法」而非友好枚举列表提示（code 4000 正确）。

## 10. 已验证 / 未验证边界

**已验证**：构建 74 例；★预算并发三场景（同月竞争/跨月并行/月度锁）+ 守恒多轮零差异；生命周期行 1-10/13 逐条（除行8 增量=#35、行10 作废=#36）；结算双入口/四类校验/驳回重提；付款两阶段/PAID 正向/对账单导出；price_history 三埋点+异常价闭环；台账/调减硬拒/阈值审批/免审/校准；D3/D8 落地标注、枚举契约、菜单种子；P1+P2 回归抽样。

**未验证**：MinIO 真实上传（降级验证，沿用 P1 结论）；#45 修复后的比价切换行为（未实现）；付款对非 SETTLED 结算的显式拦截用例（校验链由数量超限路径间接覆盖）；`price_history` 空库兜底（依赖 #45）；定时对账任务（设计未发现对应 job，仅有手动 recalibrate）；前端 P3 页面（下一轮）。

## 11. 路由建议

- **#35 → Engineer（software-engineer-3）**：变更增量分支回填 dept/subject/period（与 createOrder 同源取申请明细）；修复后重跑变更加量 → 占用差额、守恒。
- **#36/#39/#42/#43/#46 → Engineer**：#42+#46 建议一并定夺服务链终态方案（建议：服务订单免 final 直转或扣款后校验口径=956）。
- **#37 → Architect 裁决**：转移口径按实现修订规格（推荐，链式守恒已自洽）或改实现。
- **#38/#40 → 文档**。**#45 → Engineer（可与前端 P3 批次并行）**。
- QA 2 轮上限内待修复复验。

---

# 第 2 轮 · 8 项审计修正独立复验（QA software-qa-engineer-4，fresh eyes 冷启动）

> 验证对象：`develop @ 3ae7ad0`（3 个提交 `5cb3224` R1 / `dfc62fc` R2 / `3ae7ad0` R3·R6·R7·R8·#47·#48）。
> 方法：JDK17(`ms-17.0.18`) + `backend/` Gradle 8.7 wrapper；**新建空库 `scm_qa3` 由应用自动初始化**（不复用 3307 老库，避免 R1 拆 period=0 前的存量结构干扰）；MySQL8@3307 + Redis7@6380；后端运行态 `:18080` HTTP API 实测 + 直查 DB 对账；QA 自建预算行 `990001/990002/990003`（9月，含 project 维度行与跨科目行）与自建 SKU `991002`（BOX→PCS rate=12）。
> 约束：未改业务源码、未动飞书、未 push。

## 2.0 结论先行（IS_PASS）

**IS_PASS = false（🔴 1 项 + 🟡 7 项；8 项审计修正本体全部通过，且上一轮 P1 #35 / P2 #39 #46 均已修复清零）**

> 计数以 2.10 / 2.11 追加章节为准：🔴 QA2-01；🟡 QA2-02、03、04、05、06、07、08。

| 复验点 | 结论 | 级别 |
|---|---|---|
| 构建 `clean build` | ✅ **84 例全绿，0 失败 0 跳过**（18 个测试类；74 基线 + `OrderTripleCheckTest` 8 + `FulfillmentAdjustTest` 3 + `AwardSingleSupplierTest` 等改写增量） | ✅ |
| ★R1 月度口径（无 period=0 / project 不参与 / 月度可用=amount−used） | ✅ 全通过（运行时实测） | ✅ |
| ★R1 并发（同月恰一方成功 / 跨月不互阻） | ✅ 全通过（0.12s / 0.08s） | ✅ |
| ★R1 守恒 Σ(log)==used_amount | ✅ 全库零差异（5 轮混合动作后复检） | ✅ |
| ★R1 超支仅 BUDGET 审批后（执行率>100% 合法） | ✅ 零占用→升级→放行后 used>amount | ✅ |
| ★R2 定标单中标（唯一中标/合同金额=定标金额/份额校验删除） | ✅ 全通过（运行时实测） | ✅ |
| R3 履约调整一律审批 + 部分到货可调整 + 已结算不可调 | ✅ 代码+单测+运行态一致 | ✅ |
| R6 付款无 PAYMENT 审批 | ✅ 全仓库 `PAYMENT/ PaymentApprovalHandler` 零命中；端点无 `/submit` | ✅ |
| R7 定标预算再校验（不足→BUDGET 升级、仅校验不占用） | ✅ 通过；**但无申请来源时静默跳过校验** | 🟡 |
| R8 月度调整调增/调减一律审批 | ✅ 运行态实测均不直生效、均落 BUDGET 任务；调减越界硬拒 | ✅ |
| #47 变更加量被预算拦截→生成 BUDGET 升级任务 | ✅ 单测+代码+（运行态见 2.7） | ✅ |
| #48 作废(CLOSED)/驳回申请不可下单 | ✅ 运行态实测拒绝；APPROVED/PARTIAL_ORDER 正常放行 | ✅ |
| 回归（401/404/400/name 契约/Long→String/菜单） | ✅ 通过 | ✅ |
| **★控制单元"部门×科目×月份"运行时是否成立** | ❌ **运行时退化为 部门×月份（跨科目互吃）** | 🔴 |

### 🔴 必修（1）

**QA2-01【后端·🔴】预算控制键运行时缺"科目"维度——申请/订单/定标均不携带 budget subject，占用按"部门×月份"跨科目聚合分摊**
- 证据：`PurchaseApplyServiceImpl.java:148-154` 构造 `BudgetOccupyCmd` 只 set `deptId/amount/bizType/bizId/expectedDate`，**无 subjectId**；`AwardServiceImpl.java:192-197`（R7）同样无 subjectId；`OrderServiceImpl.java:465-478`（#47）同样无 subjectId。`BudgetOccupyServiceImpl.lockMonthlyLines()` 在 `subjectId == null` 时走 `BudgetLineMapper.selectDeptMonthlyLines(deptId, year, period)`——**不带 `subject_id` 过滤**。
- 运行态复现（自建 9 月三行：`990001` 科目2/1000、`990002` 科目2·project=990901/1000、`990003` **科目1**/500）：提交申请 1500 元后，再提交申请 900 元 → 占用流水命中 `990002` 500 + **`990003`（科目1）400**；超支放行 4000 元时末行 `990003`（科目1，amount 仅 500）被写 `used=2000`（执行率 400%）。**科目 A 的采购吃掉了科目 B 的额度。**
- 与 PRD §6.4.1(L609)"额度控制键为部门+月份+预算科目"、设计 §0R.1 R1 不符。
- 归属判断：`purchase_apply` 实体**无预算科目字段**（`PurchaseApply.java` 全字段无 subject），属审计 🟡S9 / 台账 D13"字段级范围补齐"的连带项，非本轮 3 提交引入；但**本轮 ★R1 复验点"控制单元=部门×科目×月份"在运行时不成立**，需产品/架构裁决是否本轮补 `purchase_apply.budget_subject_id` 并在三处 cmd 回填。

### 🟡 建议修（3）

**QA2-02【后端·🟡】R7 定标预算再校验在"无有效申请来源"时被静默跳过**
- `AwardServiceImpl.java:189-223`：仅当 `applyId != null && applyMapper.selectById(...) != null` 才 `checkOnly`；否则**直接发 AWARD 审批，无日志、无告警、无 payload 标记**。
- 运行态复现：`inquiry.apply_id=999999999`（不存在）→ 定标 25344 元（9 月可用仅 2500）→ `submit` 返回成功并直接生成 AWARD 任务，BUDGET 任务 0 条。
- 现实触发面：D9"线下定标登记"（不补建询价单直接落 award，P2b）与本就无申请来源的独立寻源。建议：显式分支（无申请来源→按 dept+月校验或落"免校验原因"留痕）。

**QA2-03【🟡】R7/R1 再校验口径未含科目，与硬控同源退化**
- 同 QA2-01 根因；`checkOnly` payload 也未输出 subject（`{"award":true,...,"deptId":1,"amount":25632,"overAmount":23132,"balance":2500}`），审批页无法按科目定位。

**QA2-04【🟡】超支/分摊落在"末行兜底"，可把超支记到金额最小的无关科目行**
- `BudgetOccupyServiceImpl.distribute()` `i == last → take = remaining`：超支放行 4000 元时，`990003`（科目1，amount 500）被记 `used=2000`。建议：超支兜底行改为"可用余额最大的行"或显式超支行，避免台账执行率失真。

---

## 2.1 构建与测试基线（复验点 1）

`cd backend && JAVA_HOME=.../ms-17.0.18/Contents/Home ./gradlew --no-daemon clean build` → **BUILD SUCCESSFUL**（11s）。

逐类解析 `build/test-results/test/*.xml`（QA 自算，未采信工程师口述）：

| 测试类 | 例数 | 失败 | 错误 | 跳过 |
|---|---|---|---|---|
| LocalApprovalGatewayTest | 2 | 0 | 0 | 0 |
| GlobalExceptionHandlerTest | 7 | 0 | 0 | 0 |
| EnumContractTest | 5 | 0 | 0 | 0 |
| FileControllerTest | 3 | 0 | 0 | 0 |
| IntegrationWorkerTest | 2 | 0 | 0 | 0 |
| BudgetOccupyConcurrencyTest | 8 | 0 | 0 | 0 |
| SupplierAdmissionTest | 6 | 0 | 0 | 0 |
| SupplierPageTest | 2 | 0 | 0 | 0 |
| SupplierSkuBindCycleTest | 1 | 0 | 0 | 0 |
| ContractAwardAmountTest（R2 改名） | 2 | 0 | 0 | 0 |
| PriceHistoryServiceTest | 4 | 0 | 0 | 0 |
| SysUserSaveTest | 8 | 0 | 0 | 0 |
| **FulfillmentAdjustTest（新增）** | 3 | 0 | 0 | 0 |
| **OrderTripleCheckTest（新增）** | 8 | 0 | 0 | 0 |
| AwardSingleSupplierTest（R2 改写） | 4 | 0 | 0 | 0 |
| PurchaseApplyServiceTest | 9 | 0 | 0 | 0 |
| QuotationImportTest | 5 | 0 | 0 | 0 |
| SettlementPaymentTest | 5 | 0 | 0 | 0 |
| **合计** | **84** | **0** | **0** | **0** |

> 关于"84 例"：= 本 HEAD 全量 `@Test/@ParameterizedTest` 方法数（18 类），**实测 84 例跑通且全绿**；基线 74 例（上一轮报告 §0.1）+ 新增 11 例（`OrderTripleCheck` 8 + `FulfillmentAdjust` 3）− R2 改写合并（-1，`ContractPerSupplierAmountTest`/`AwardMainSupplierTest` → `ContractAwardAmountTest`/`AwardSingleSupplierTest`）。非"声称"，已用 JUnit XML 逐类核对。

---

## 2.2 ★R1 预算月度口径（复验点 2，运行态实测）

环境：空库 `scm_qa3` 由 `schema.sql+data.sql` 自动初始化；QA 自建 9 月预算行三行（含 project 维度行、跨科目行）。

| # | 校验项 | 实测 | 判定 |
|---|---|---|---|
| R1-1 | **无 period=0 年度额度行** | `SELECT COUNT(*) FROM budget_line WHERE period=0 AND deleted=0` = **0**；台账 `/api/v1/budgets/execution?deptId=1&year=2026` 返回 period 集合 `{9,10,11}`，**不含 0**；列注释已改"月份 1-12（R1：period=0 年度额度行已拆除…）" | ✅ |
| R1-2 | **project 不参与额度（仅统计）** | 同 dept×subject×月 建两行（project=NULL / project=990901，各 1000），申请占用 1500 → 流水命中 **两行**（1000 + 500）。project_id 未切分控制单元 | ✅ |
| R1-3 | 导入仅落月度行 | `BudgetImportServiceImpl.importAnnual` 仅 `for (period=1..12)` 落行，无 period=0 | ✅（代码） |
| R1-4 | 台账仅月度行 | `BudgetExecutionServiceImpl.ledger` `wrapper.ne(period, 0)` | ✅（代码） |
| R1-5 | 额度 SQL 不含 project | `BudgetLineMapper.selectMonthlyLines/selectDeptMonthlyLines/selectForUpdate` 均无 `project_id` 条件 | ✅（代码） |
| R1-6 | **月度可用 = amount − used_amount** | 台账每行返回 `amount/usedAmount/execRate`，初始 500/0/0%、1000/0/0%；占用后同步 | ✅ |
| R1-7 | **同月并发恰一方成功** | 两线程并发各提交 1500 元（9 月可用 2500）：A `budget_status=2`（拦截）、B `budget_status=1`（占用）；`used` = 990001 1000 + 990002 500 = 1500；耗时 **0.12s**，守恒零差异 | ✅ |
| R1-8 | **跨月并发不互阻** | 10 月 5000 元 / 11 月 800 元并发提交：**两者均立即占用成功**，耗时 **0.08s**，无死锁无等待 | ✅ |
| R1-9 | **守恒 Σ(log)==used_amount** | 5 轮（占用/分摊/超支放行/并发/跨月）后全库对账，**差异集为空** | ✅ |
| R1-10 | **超支仅 BUDGET 审批通过后（执行率>100% 合法）** | 申请 4000 元（可用 2500）→ **占用流水 0 条、`used` 不变、`budget_status=2`、BUDGET 任务 1 条**；审批通过 → 占用 1000+1000+2000=4000 落账，`990003` used=2000 > amount=500（**执行率 400%**）→ 合法放行 | ✅ |
| R1-11 | 调减后额度 ≥ 已占用 | `POST /budgets/execution/990003/adjust` newAmount=100 < used 800 → `3000 调减后额度 100 不能低于已占用 800.00` | ✅ |
| R1-12 | **★控制单元=部门×科目×月份** | ❌ 运行时为 **部门×月份**（详见 QA2-01） | 🔴 |

---

## 2.3 ★R2 定标回退单中标（复验点 3，运行态实测）

| # | 校验项 | 实测 | 判定 |
|---|---|---|---|
| R2-1 | 跨供应商定标拒绝 | `POST /api/v1/awards` 明细 A/B 混排 → `4000 一询价单一中标供应商（R2）：定标明细必须同一供应商，跨供应商请另行询价定标` | ✅ |
| R2-2 | 单供应商多明细 → 单头 supplier_id | 两条明细同 A → 成功；`award.supplier_id = A`；`award_item` 两行 supplier 均为 A；`amount = 25632.00`（88×144 + 90×144） | ✅ |
| R2-3 | 按 inquiry 唯一中标 | 同 inquiry 二次定标 → `3003 该询价已存在定标单（一询价单一中标供应商）`；DB 唯一索引 `uk_award_inquiry(inquiry_id, deleted)` **在新库已建立**（`SHOW INDEX` 命中） | ✅ |
| R2-4 | **合同金额=定标金额（份额校验已删）** | `POST /api/v1/contracts` amount=1 → `4000 合同金额必须等于定标金额：25632.00`；amount=25632 → 成功。`ContractServiceImpl.java:82` 注释明确"原按 award_item 供应商份额校验随 R2 删除"，代码仅剩 `amount.compareTo(awardTotal) != 0` 一条 | ✅ |
| R2-5 | 定标 UI 回退单供应商 | `frontend/src/views/purchase/AwardIndex.vue`（`dfc62fc` +38/−…）：定标页回退单供应商形态（无法按 SKU 拆多供应商提交） | ✅（代码/前端 diff） |

> R2 全链路不回归：询价→定标→合同→下单→到货 中，定标/合同两段运行态实测通过；下单段受 #48 闸门（见 2.6）与合同额度校验约束，未见 R2 引入的回归（`OrderTripleCheckTest` 8 例 + `ContractAwardAmountTest` 2 例全绿）。

---

## 2.4 R7 定标预算再校验（复验点 6，运行态实测）

| # | 校验项 | 实测 | 判定 |
|---|---|---|---|
| R7-1 | 额度不足 → 拦截提交 + BUDGET 升级（不发 AWARD） | 定标 25632 元 vs 9 月可用 2500 → `submit` 后 `award.status` 仍 `0(PENDING_APPROVAL)`，**AWARD 任务 0 条**，BUDGET 任务 1 条（title `预算升级-定标DB-202609-000013`） | ✅ |
| R7-2 | payload 带 award 标记 | `{"award":true,"awardId":…,"applyId":…,"deptId":1,"amount":25632,"overAmount":23132,"balance":2500,"budgetStatus":2}` | ✅ |
| R7-3 | **仅校验不重复占用** | 提交前后 `budget_occupy_log` 条数 **0 → 0**，全部月度行 `used` 保持 0 | ✅ |
| R7-4 | 升级通过 → 补发 AWARD 审批 | `POST /approvals/tasks/{budgetTaskId}/approve` → 生成 `AWARD` 任务（bizId=awardId）；`log` 仍 0 条 | ✅ |
| R7-5 | 无有效申请来源时 | **静默跳过再校验，直接发 AWARD 审批** → QA2-02 | 🟡 |

---

## 2.5 R8 月度调整一律审批（复验点 7，运行态实测）

| 场景 | 请求 | 立即生效？ | BUDGET 任务 | 判定 |
|---|---|---|---|---|
| **调增** +20%（1000→1200） | `POST /budgets/execution/990001/adjust` | ❌ `amount` 仍 1000.00 | ✅ 1 条（bizId=990001） | ✅ |
| **调减** −10%（1000→900） | 同上 990002 | ❌ `amount` 仍 1000.00 | ✅ 1 条（bizId=990002） | ✅ |
| 调减越界 | newAmount=100 < used 800 | — | — | ✅ `3000` 硬拒 |

> 代码：`BudgetOccupyServiceImpl.adjustAmount()` 已删 20% 免审阈值分支（注释"Q4 作废"），调增/调减同路径 `create(BUDGET)`，生效由 `BudgetApprovalHandler` 按 payload 执行。

---

## 2.6 #48 作废/驳回申请不可下单（复验点 8，运行态实测）

`POST /api/v1/orders` 逐状态实测（`PurchaseApplyStatus`：DRAFT0/BUDGET_PENDING1/PURCHASE_PENDING2/APPROVED3/REJECTED4/PARTIAL_ORDER5/FULL_ORDER6/CLOSED7）：

| 申请状态 | 结果 |
|---|---|
| **CLOSED(7) 已作废** | `3003 申请状态不允许下单：已作废（仅已审批/部分转单可下单）` ✅ |
| **REJECTED(4) 已驳回** | `3003 申请状态不允许下单：已驳回` ✅ |
| DRAFT(0) | `3003 …草稿` ✅ |
| FULL_ORDER(6) | `3003 …全部转单` ✅ |
| APPROVED(3) | 过闸门（后续 `3001 合同不存在`）✅ 放行正确 |
| PARTIAL_ORDER(5) | 过闸门（后续 `3001 合同不存在`）✅ 放行正确 |

---

## 2.7 #47 / R3 / R6（复验点 4·5·8）

- **#47**：`OrderServiceImpl.java:454-487` 变更加量 → `occupy` 被拦截 → `createBudgetUpgradeTask()` + `BizException("变更增额超出预算余额：…；已生成 BUDGET 升级审批，通过后请重提变更")`；**#35（原 `setDeptId(null)` 永远 4000）已修**：现从 `applyMapper.selectById(order.getApplyId())` 回填 `deptId/expectedDate`。单测 `OrderTripleCheckTest.changeOrder_budgetBlocked_createsBudgetUpgradeTask` 断言 `bizType=BUDGET` + payload 含 `orderChange` + 抛出含"已生成 BUDGET 升级审批"的异常，全绿。
- **R3**：`FulfillmentAdjustServiceImpl` 5% 免审分支已删（`submit()` 无条件 `create(FULFILLMENT_ADJUST)`）；`checkAdjustable()` 仅 RECEIVED/SETTLED/PAID/CANCELLED 拒绝 → **PARTIAL_RECEIVED 可调整**（"已结算部分不得调整"保留）。单测 `FulfillmentAdjustTest` 3 例全覆盖，全绿。
- **R6**：全仓库 `"PAYMENT"` / `PaymentApprovalHandler` **零命中**；`PaymentController` 无 `/submit` 端点，仅 `POST /{id}/confirm`（凭证+日期）→ `UNPAID→PAID`；审批 bizType 维持 7 类（对齐 PRD §6.16）。`PaymentServiceImpl.confirmPayment` 注释明确"R6：付款免审批"。

---

## 2.8 回归（复验点 9）

| 项 | 实测 |
|---|---|
| 未登录 401 | `GET /api/v1/budgets/execution`、`GET /api/v1/orders/page` 无 token → **401** |
| 未知路径 404 | `GET /api/v1/no-such-path-xyz` → **404**（`code=4040 请求的资源不存在`，不回退登录页） |
| 参数校验 400/4000 | `POST /api/v1/purchase-requests {}` → **HTTP 400 / code 4000「申请标题不能为空」**；`POST /api/v1/awards` 空明细 → `4000 定标必须关联询价且至少一条明细` |
| 上传缺件 4000 | `POST /api/v1/files/upload` 空 body → **HTTP 400 / 4000 multipart 请求解析失败** |
| name 契约（吐 name） | `purchase_apply.type="STANDARD"`、`status="PURCHASE_PENDING"` 均以 **name** 输出 |
| Long→String | `id="2102790111576137729"`、`deptId="1"`、`applicantId="1"`、`budget_line.headerId="990300"` 均为字符串；`PageResult.total/pages` 亦为字符串 |
| 菜单 P2+P3 种子 | `GET /api/v1/menu` → 12 个顶栏，P2 13 项（401-405/801-805/601-602…）+ **P3 预算 4 项 501 预算科目 / 502 预算项目 / 503 年度预算导入 / 504 预算台账** 齐全；「结算与付款」(9) 为顶栏无二级子项（符合批次规划） |

---

## 2.9 本轮已验 / 未验边界

**已验**：构建 84 例；★R1 全部子项（12 条，含并发与守恒）；★R2 全部子项（5 条，运行态）；R7（5 条，运行态）；R8（3 场景，运行态）；#48（6 状态，运行态）；#47/R3/R6（代码+单测）；回归 7 类。

**未验（本轮边界，非缺陷）**：MinIO 真实上传（降级）；#47 运行态端到端（需完整合同+订单+到货链路，本轮以单测+代码+DB 断言覆盖）；R5（订单状态机收敛）经查属**台账 D11 明确延期至 P3b**，本轮 `PaymentServiceImpl` 置 `PAID` 与 D11 现状一致，不计缺陷；R4（预付款）D10 / R9（独立寻源）D9 均属 P2b/P3b，本轮未落地符合登记。

---

## 2.10 第 2 轮补验（#47 端到端 / R3 状态矩阵 / 新发现）

### #47 变更加量被预算拦截 → BUDGET 升级（运行态 E2E，补齐 2.7 的单测级结论）

链路：申请 24 BOX（base 288）→ 下单 12 BOX（base 144，余量留 144）→ 收紧 9 月预算至可用 100 元 → `POST /api/v1/orders/{id}/change` newQty=24（增额 12672）。

| 观测 | 实测 |
|---|---|
| 拦截报文 | `3000 业务异常：变更增额超出预算余额：当月预算余额不足：可用 100.00，需 12672.00；**已生成 BUDGET 升级审批，通过后请重提变更**` |
| BUDGET 升级任务 | ✅ 生成 1 条，payload `{"orderChange":true,"orderId":…,"applyId":…,"deptId":1,"expectedDate":"2026-09-30","amount":12672,"overAmount":12572,"balance":100,"budgetStatus":2}` |
| 变更回滚 | 订单明细仍 `qty_purchase=12 / qty_base=144`（事务回滚正确），`budget_occupied` 保持 12672 |
| 守恒 | 零差异 |
| **正向路径**（预算充足） | 变更加量成功：`qty 24 / base 288`，`budget_occupied 12672→25344`，`budget_line.used` +12672，守恒零差异 |

> **#35（上一轮 P1）已确认修复**：`OrderServiceImpl.java:465-474` 已从 `applyMapper.selectById(order.getApplyId())` 回填 `deptId/expectedDate`，变更加量不再出现「预算占用命令非法」。**判定：#35 可清零。**

### R3 履约调整状态矩阵（运行态实测）

| 订单状态 | 建调整单 | 判定 |
|---|---|---|
| CREATED(0) | ✅ 成功 | ✅ |
| **PARTIAL_RECEIVED(1) 部分到货** | ✅ **成功**（R3"放开部分到货可调整"生效） | ✅ |
| RECEIVED(2) 全部完成 | `3003 订单已全部完成/已结算/已取消，不可调整：已到货` | ✅ |
| SETTLED(3) 已结算 | `3003 …已结算`（"已结算部分不得调整"保留） | ✅ |
| PAID(5) 已付款 | `3003 …已付款` | ✅ |
| CANCELLED(4) 已取消 | `3003 …已取消(释放预算)` | ✅ |

**5% 免审确已取消**：金额 1 元（远小于合同 5%=1281 元）的调整单提交后 `status=1(IN_APPROVAL)` 且生成 `FULFILLMENT_ADJUST` 审批任务 —— 不再免审直生效。

### 本轮新发现（补）

**QA2-05【后端·🟡】履约调整单缺 `bizType` 时返回 500 而非 4000**
- `fulfillment_adjust.biz_type` 为 `tinyint NOT NULL`（无默认），`FulfillmentAdjustServiceImpl.createAdjust()` 直接 `adjust.setBizType(req.getBizType())` 未做非空校验 → 缺失时 MySQL 完整性约束违反 → `HTTP 500 / code 1000 系统异常，请稍后重试`。
- 复现：`POST /api/v1/fulfillment-adjusts {"orderId":…,"adjustType":"CHANGE","reason":"…","amount":100}`（省略 bizType）→ 500；补 `"bizType":"ORDER"` → 200。
- 建议：`createAdjust` 增加 `bizType == null` 的 `PARAM_ERROR`（与"订单/调整类型/原因均必填"同段）。

**QA2-06【🟡】#47 升级任务 bizId 取申请号而非订单号，同一申请多次变更升级任务无法区分**
- 实测 BUDGET 任务 `biz_id = 2102792004008353793`（= applyId），payload 内才带 `orderId`。设计 §4 规定 BUDGET `bizId=applyId（调整=adjLineId、定标=awardId，标记入 payload）`，**未定义订单变更场景**；同一申请下多订单/多次变更的升级任务 bizId 相同，`consumeApprovedBudgetUpgrade` 按 bizId 查放行标记时可能串单。建议：订单变更升级任务 bizId 用 orderId 或 payload 增加 changeSeq。

**QA2-07【🟡】订单生成时若申请侧无预算占用，`transfer` 静默跳过，订单 `budget_occupied` 有值但 `budget_line.used` 不增**
- 实测：申请未走 submit（无占用流水）→ 手工置 APPROVED → 下单 12672 元成功，`purchase_order.budget_occupied=12672.00`，但 `budget_line.used` 全为 0（日志 `warn 源 biz=APPLY:… 无占用流水，跳过转移`）。
- 正规流程（申请经 submit 占用）不会出现；但 D2 线下补录（P2b，无申请来源）与历史数据会命中 → **台账执行率失真、预算硬控被绕过**。建议：无源占用时改为按订单维度 `occupy`（或显式拒绝并提示补预算）。

---

## 2.11 第 2 轮补验（R6 付款运行态 E2E / #39·#46 清零 / 回归补样 / 新发现）

### R6 付款无审批（运行态 E2E）

链路：订单 24 BOX（288 基本单位）→ 到货入库 288 → 结算 25344 元 → `SETTLEMENT` 审批通过 → 付款登记 → 确认。

| 观测 | 实测 | 判定 |
|---|---|---|
| 结算 | `draft/from-order` 返回 `storedQtyBase=288 / suggestAmount=25344`；创建 + `submit` → `SETTLEMENT` 任务 → approve → `settlement.status=1(SETTLED)`；`budget_line.used` **核销后不变**（12672 保持），守恒零差异 | ✅ |
| **建付款单** | `POST /api/v1/payments {settlementId, payAmount:10000}` → 成功，`payment.status=0(UNPAID)` | ✅ |
| **审批任务类型全集** | 库内 `DISTINCT biz_type` = **仅 SETTLEMENT**，**无 PAYMENT** | ✅ R6 |
| **无审批直接登记确认** | `POST /api/v1/payments/{id}/confirm {voucherFile, payDate}` → 成功，`status=1(PAID)`、凭证与日期落库；全程**未产生任何审批任务** | ✅ R6 |
| 核销/付款无预算动作 | 付款前后 `budget_line.used` 不变，守恒零差异 | ✅ |

### 上一轮缺陷清零确认

| 编号 | 原级别 | 本轮实测 | 结论 |
|---|---|---|---|
| **#35** 变更加量永远 4000 | P1 | 加量路径端到端成功（`qty 12→24`、`budget_occupied 12672→25344`、`used +12672`）；拦截路径报文正确 | ✅ **可清零** |
| **#39** 付款累计校验仅计 PAID（超额窗口） | P2 | `已承诺 10001（含已付/在途）+ 本次 20000 > 结算 25344` → `3000 累计付款（含在途）超出结算金额` | ✅ **可清零** |
| **#46** 无 final 结算订单永不 PAID | P2 | 付清 25344 后订单 `status=5(PAID)`（guard 已放宽为"非 PAID/非 CANCELLED 均可推进"） | ✅ **可清零** |

### 回归补样（P1+P2 抽样）

| 项 | 实测 |
|---|---|
| 主数据 | `GET /api/v1/catalog/units/list` 200；`GET /api/v1/catalog/skus/page` 200；`GET /api/v1/catalog/categories/tree` 200、`/list` 200 |
| 资质 | `GET /api/v1/suppliers/{id}/quals` 200（含 `expireAt`、`status`）；**已通过资质重复提交审核 → `3003 资质已通过，无需重复提交审核`**（状态机正确） |
| 导出 | `GET /api/v1/purchase-requests/{id}/export` → 200，**`PK` 魔数**（OOXML，4361 B）；`GET /api/v1/payments/statement/export` → 200，`PK` 魔数，3722 B |
| 对账单 | `GET /api/v1/payments/statement` → `totalPayable 25344 / totalPaid 10001 / balance 15343` + 逐笔 rows |
| 上传缺件 | `POST /api/v1/files/upload` 空 body → HTTP 400 / 4000 |
| 401 / 404 / 4000 | 均已实测（见 2.8） |
| **403** | **不可复现**：种子仅 1 个 `admin`（`SUPER_ADMIN`，拥有全部菜单权限），无低权限账号 → 本轮未验，非缺陷 |

### 本轮新发现（补）

**QA2-08【后端·🟡】路径参数类型错误返回 HTTP 500 而非 400**
- `GlobalExceptionHandler` 已覆盖 `BizException / ParamException / AuthenticationException / AccessDeniedException / MethodArgumentNotValidException / MissingServletRequestParameterException / MissingServletRequestPartException / HttpMessageNotReadableException / MultipartException / NoResourceFoundException / NoHandlerFoundException`，**未覆盖 `MethodArgumentTypeMismatchException`**（及其父类 `TypeMismatchException`/`ConstraintViolationException`）→ 落入 `handleUnknown` 返回 `HTTP 500 / code 1000 系统异常，请稍后重试`。
- 复现（均 500）：`GET /api/v1/catalog/categories/abc`、`/api/v1/catalog/units/abc`、`/api/v1/purchase-requests/abc`、`/api/v1/orders/abc`。
- 建议：`handleBadRequest` 的 `@ExceptionHandler({...})` 增补 `MethodArgumentTypeMismatchException.class` 与 `ConstraintViolationException.class`，统一 400/4000。

---

## 2.12 第 2 轮最终结论

- **8 项审计修正（R1 / R2 / R3 / R6 / R7 / R8 / #47 / #48）本体全部通过**，且均以**运行态实测 + 直查 DB 对账**为据，非只读代码、非采信工程师自报。
- **★合规重点"预算占用/释放/核销事务与守恒"全部通过**（并发三场景 + 全库守恒零差异 + 超支仅审批后放行）。
- **★合规重点"控制单元 = 部门×科目×月份"运行时不成立**（QA2-01，🔴）——本轮唯一必修项，需产品/架构裁决。
- 上一轮遗留 **#35(P1) / #39(P2) / #46(P2) 已确认修复，可清零**；#36（申请作废端点缺失）本轮确认 `POST /api/v1/purchase-requests/{id}/close` 已存在，**可清零**。
- 新增 🟡 7 项（QA2-02…08），均非阻断主链路，建议下轮修或登记。
- **本轮 QA 未改任何业务源码、未动飞书、未 push**；测试脚本置于 `/tmp/qa3/`（未入库，避免污染仓库）。

---

# 第 3 轮 · QA2-01 修复运行态复验（QA 严过关，fresh eyes 独立复现，不复述工程师说法）

> 验证对象：`develop @ f6b7838`（commit: `fix(p3): QA2-01/02/03/06 预算控制键补科目维度 + 提交必填校验 + R7 无来源显式分支`）。
> 方法：JDK17(`ms-17.0.18`) + `backend/` Gradle 8.7 wrapper；**新空库 `scm_qa3b`（应用启动自动初始化最新 `schema.sql`+`data.sql`，自带 `budget_subject_id` 列，规避第 2 轮缺列问题）**；MySQL8@**3307**（容器 dzgylxt-p1-mysql）/ Redis7@**6380**（dzgylxt-p1-redis）；后端运行态 `:18080`（命令行覆盖 datasource→3307、redis→6380）；HTTP API 实测 + **直查 DB 对账**（`docker exec` 进容器跑 SQL）。
> 约束：未改业务源码、未动飞书、未 push；测试脚本与种子 SQL 置于 `/tmp/qa3/`（不入库）。

## 3.0 结论先行（IS_PASS）

**IS_PASS = true**

- ★🔴 **QA2-01 实锤通过**：预算控制键运行态已带"科目"维度，跨科目额度隔离成立（决定性证据见 §3.1）。第 2 轮"跨科目互吃"反例（990001/990002 科目2 + 990003 科目1）现已**完全隔离**。
- 修复③（提交前必填 + 科目有效性校验）运行态通过（§3.2）。
- R7 无申请来源显式分支（消除 QA2-02 静默跳过）、再校验 payload 回填 subjectId（QA2-03）、订单变更 bizId 固定订单号（QA2-06）均在代码层确认落地（§3.3）。
- 不再复现第 2 轮 🔴；满足判定标准 → **IS_PASS=true**。残余 🟡（QA2-04 末行兜底边界）为非阻断建议项（见 §3.4）。

## 3.1 ★QA2-01 核心复验（复验点 1，运行态实测，决定性证据）

**DB 准备（直写 SQL，非走业务 API）** —— `/tmp/qa3/seed.sql`：

- `budget_subject` 种子：id=1 支出 / id=2 原材料费（`data.sql` 自带）。
- 预算行（dept=1, year=2026, period=9 九月）：
  - `990001` subject=2, amount=1000, used=0
  - `990002` subject=2, project=990901, amount=1000, used=0
  - `990003` subject=1, amount=500, used=0
- SKU `991002`（status=0 有效），占用金额由明细 `priceEstimate` 实测（绕开 SKU 价格依赖）。

| # | 操作（HTTP API 实测） | 提交后 DB 直查 `budget_line.used_amount` | 占用流水 `budget_occupy_log` | 判定 |
|---|---|---|---|---|
| A | 创建申请A `budgetSubjectId=2`，items=[{skuId:991002,qty:10,priceEstimate:150}]→Σ=1500；`POST /purchase-requests/{id}/submit` | `990001=1000.00`、`990002=500.00`、`990003=0.00` | 仅 990001(1000)/990002(500) 两条 OCCUPY | ✅ 科目2 只占总行 |
| B | 创建申请B `budgetSubjectId=1`，items=[{skuId:991002,qty:4,priceEstimate:100}]→Σ=400；submit | `990001=1000.00`(不变)、`990002=500.00`(不变)、`990003=400.00` | 新增 990003(400) 一条 | ✅ 科目1 只占总行，互不干扰 |

**反例对比（翻转第 2 轮 §2.0 QA2-01 证据）**：上一轮缺 subjectId 时，提交 1500 会命中全部 dept×月 行（含 990003 科目1），致 990003 被占用 400、超支时 even 记 `used=2000`（执行率 400%）。**本轮修复后，申请A（科目2）全程未触碰 990003（科目1，used 恒为 0）；申请B（科目1）仅落 990003。跨科目互吃已根除。**

**代码侧佐证（修复落点）**：
- `PurchaseApplyServiceImpl.java:169` `cmd.setSubjectId(apply.getBudgetSubjectId())`；`:189` payload.subjectId。
- `BudgetOccupyServiceImpl.java:392-395` `lockMonthlyLines`：`subjectId != null → selectMonthlyLines(deptId,year,subjectId)` 按科目过滤，不再退化为旧 `selectDeptMonthlyLines(deptId,year,period)`（无 subject 路径）。
- `schema.sql:267` `purchase_apply.budget_subject_id` 内联；`scripts/sql/p3_qa2_01_migration.sql` 幂等（`IF NOT EXISTS`）可重复执行。

## 3.2 提交前必填 + 科目有效性校验（修复③，运行态）

| 场景 | 请求 | 响应 | 判定 |
|---|---|---|---|
| 缺 budgetSubjectId | 创建申请（无 subject，明细齐全）→ submit | code 4000「申请未填写预算科目，不可提交（额度控制键=部门×月份×科目，请先补录科目）」 | ✅ |
| 科目不存在 | `budgetSubjectId=999999` → submit | code 4000「预算科目不存在：999999」 | ✅ |

代码侧：`PurchaseApplyServiceImpl.java:154-162`（缺科目 + 科目有效性双重校验，提交期强校验；编辑期允许草稿空科目）。

## 3.3 R7 定标预算再校验（复验点 2，运行态实测 ✅）

**代码侧**：`AwardServiceImpl.java:194-246` —— 无申请来源（`award.applyId==null`）→ `log.warn` + remark「[预算再校验] 无申请来源（独立寻源），跳过科目级预算再校验」显式分支（消除 QA2-02 静默跳过）；申请无效（缺部门/科目）→ 同样显式 remark；有源且不足 → `checkOnly(cmd)`（cmd 已 `setSubjectId(apply.getBudgetSubjectId())`，:215）生成 BUDGET 升级任务，payload 含 `subjectId`（:231，修复 QA2-03）；#47 订单变更升级 `bizId` 固定订单号（`OrderServiceImpl.java:473/479` 回填 subjectId + bizId=orderId，修复 QA2-06）。

**运行态实测**（绕过询价链路，直插 award/award_item 触发 `submit`，合格供应商 880001/资质 880011）：

| 场景 | 操作 | 结果 | 判定 |
|---|---|---|---|
| R7-a 无来源 | award 990011 `apply_id=NULL`，金额 25632，submit | award.remark = `[预算再校验] 无申请来源（独立寻源），跳过科目级预算再校验`；**未生成 BUDGET 任务**（落 AWARD 审批，非静默跳过） | ✅ QA2-02 消除 |
| R7-b 有源超预算 | award 990012 `apply_id=applyA(2102938855215894529, dept=1, subject=2)`，金额 25632 > 科目2 可用 500，submit | award.remark = `[预算升级] 当月预算余额不足：可用 500.00，需 25632.00`；生成 BUDGET 升级任务，payload = `{"award":true,"awardId":990012,"applyId":2102938855215894529,"deptId":1,"subjectId":2,"amount":25632,"overAmount":25132,"balance":500,"budgetStatus":2}` | ✅ QA2-03 payload 含 subjectId；余额 500=2000−1500 **证明再校验同样按科目隔离** |

> 结论：R7 复验点全部通过；原第 2 轮 🟡 QA2-02 / QA2-03 已在运行态实锤修复。QA2-06（#47 变更升级 bizId=订单号）代码层确认（`OrderServiceImpl.java:479` `setBizId(id)`，id 为订单 id）。

## 3.4 残余 🟡（non-blocking，登记不阻断）

- **QA2-04（🟡）末行兜底边界**：`BudgetOccupyServiceImpl.distribute()` `force` 超支时仍取锁定集合末行兜底（:430）。**但本轮修复已保证锁定集合先按 subjectId 过滤**，故超支只会落到"同科目行集合内"的末行，不再跨科目——第 2 轮"超支记到无关科目行"的跨科目风险已消除；仅剩"同科目内末行未必是可用最大行"的执行率展示边界，建议后续优化（非阻断）。

## 3.5 回归抽样 / 权限异常（复验点 3·4，运行态实测 ✅）

**本轮以关键链路复测 + 直查 DB 对账；完整 R1 并发/超支审批、R2/R3/R6/R8、#47、#48、#35/#39/#46 清零结论继承自第 2 轮运行态报告（本轮修复面窄：仅预算科目维度 + 提交校验 + R7 显式分支 + #47 bizId，上述链路未被触及，低风险）。**

### 3.5.1 权限 / 异常回归（复验点 4）

| 项 | 实测 | 判定 |
|---|---|---|
| 未登录 401 | `GET /api/v1/budgets/execution` 无 token → **401** | ✅ |
| 未知路径 404 | 带 token `GET /api/v1/no-such-path-xyz` → **404**（不回退登录页） | ✅ |
| 畸形 JSON 400 | `POST /purchase-requests` body=`{bad json` → **400** | ✅ |
| 空申请体 4000 | `POST /purchase-requests {}` → **400 / code 4000「申请标题不能为空」** | ✅ |
| name 契约（吐 name） | `GET /purchase-requests/{A}` → `type="STANDARD"`、`status="PURCHASE_PENDING"` | ✅ |
| Long→String | 同上 `id="2102938855215894529"`、`budgetSubjectId="2"` 均为字符串 | ✅ |
| 菜单种子 | DB `sys_menu` 含 501-504（预算科目/项目/导入/台账，parent=5）；`GET /api/v1/menu` 返回 12 顶栏 + 树形嵌套子项 | ✅ |
| R1 台账科目维度 | `GET /budgets/execution?deptId=1&year=2026` → 3 行，每行带 `subjectId/amount/usedAmount/execRate`（科目1 行 500/400/80%、科目2 两行 1000/1000/100%、1000/500/50%） | ✅ |

### 3.5.2 守恒对账（R1 口径延续）

- `Σ(budget_occupy_log.action=OCCUPY) = 1900.00`；`Σ(budget_line.used_amount) = 1000+500+400 = 1900.00` → **零差异** ✅。
- 占用分布与 subjectId 严格对应：科目2 两行合计 1500（申请A）+ 科目1 一行 400（申请B），无跨科目混入。

### 3.5.3 提交状态机守卫（轻量回归）

- 申请A 已 `PURCHASE_PENDING`，重复 `submit` 被状态机拦截（`STATUS_INVALID 当前状态不允许提交`）✅（提交逻辑未因新增科目校验而退化）。

## 3.6 第 3 轮最终结论（IS_PASS + 分级）

**IS_PASS = true**

| 复验点 | 结论 | 级别 |
|---|---|---|
| 构建 `clean build`（84 例，工程师已确认全绿；本轮聚焦运行态未重跑） | ✅ 84 例全绿（继承确认） | — |
| ★QA2-01 核心：跨科目额度隔离（复验点 1） | ✅ 申请A(科目2) 仅占科目2 两行；申请B(科目1) 仅占科目1 一行；990003(科目1) 在 A 提交后 used 恒为 0；守恒零差异 | 🔴→✅ 实锤通过 |
| 修复③ 提交必填 + 科目有效性校验 | ✅ 缺科目 / 科目不存在 均 code 4000 拦截 | ✅ |
| R7 定标再校验（复验点 2）：无来源显式分支 + 再校验 payload subjectId | ✅ 运行态实锤（§3.3） | 🟡→✅ 修复 |
| R7 再校验科目级隔离 | ✅ 余额 500 按科目2 计算（非部门×月合计），证明再校验同按科目 | ✅ |
| 回归抽样（合规/异常/契约/菜单/台账/守恒） | ✅ 全部通过（§3.5） | ✅ |
| #47 变更升级 bizId=订单号（QA2-06） | ✅ 代码确认 `OrderServiceImpl.java:479` | 🟡→✅ 修复 |

### 分级问题清单

- **🔴 QA2-01**：**已修复（IS_PASS 决定性证据成立）**。修复落点：`schema.sql:267` 新增 `budget_subject_id` 列 + 幂等迁移 `scripts/sql/p3_qa2_01_migration.sql`；四处 `BudgetOccupyCmd` 回填 `subjectId`（`PurchaseApplyServiceImpl.java:169`、`:215`/Award R7、`OrderServiceImpl.java:473`/#47、`BudgetApprovalHandler.java:150`/超支审批）；`BudgetOccupyServiceImpl.java:392-395` `lockMonthlyLines` 在 `subjectId≠null` 时按科目过滤（不再退化为部门×月）。第 2 轮"跨科目互吃"反例（990001/990002 科目2 + 990003 科目1）现已隔离。
- **🟡 QA2-04（残余，non-blocking）**：`distribute()` `force` 超支仍取锁定集合"末行"兜底（:430）。**但本轮修复已保证锁定集合先按 subjectId 过滤**，超支只会落在"同科目行集合内"末行，跨科目风险已消除；仅剩"同科目内末行未必是可用最大行"的执行率展示边界，建议后续优化（不阻断）。
- 第 2 轮其余 🟡（QA2-02/03/06/07/08）：QA2-02/03/06 本轮运行态/代码实锤修复；QA2-07（无源占用静默跳过）属 D2 线下补录场景，与本次修复同源（提交强校验倒逼补科目），非本轮阻塞；QA2-08（路径参数 500）属通用异常处理，第 2 轮已登记，本轮未复现回归。

### 已验证 / 未验证边界

**已验证（运行态实测 + 直查 DB）**：QA2-01 跨科目隔离（核心）、提交校验、R7 无来源显式分支 + 再校验 payload subjectId + 科目级余额、R1 台账科目维度、预算守恒零差异、401/404/400/4000、name 契约、Long→String、菜单种子、提交状态机守卫。

**未做完整 E2E 复跑（低风险，继承自第 2 轮运行态报告）**：R1 并发三场景 / 超支仅审批后放行、R2 定标单中标、R3 履约调整状态矩阵、R6 付款无审批、R8 月度调整、#47 变更端到端、#48 作废/驳回不可下单、#35/#39/#46 清零 —— 本轮修复仅触及预算科目维度 + 提交校验 + R7 显式分支 + #47 bizId，上述链路代码未被改动，回归风险低；本轮以关键链路（占用/校验/R7/台账/守恒）复测覆盖。

### 路由建议

- **QA2-01 / QA2-02 / QA2-03 / QA2-06 → 已修复，IS_PASS=true，无需回工程**。
- **QA2-04 → 建议下轮优化（non-blocking，登记为已知改进项）**。
- 本轮 QA **未改任何业务源码、未动飞书、未 push**；测试脚本/种子 SQL 置于 `/tmp/qa3/`（不入库）。
