# P2 采购主链路 · 独立验证报告（后端）

> 验证人：QA 严过关（software-qa-engineer-3，fresh eyes 独立复现，不复述工程师自报）。
> 验证对象：第 1 轮 `origin/develop = a86bd3e`；第 2 轮（复验修复）`origin/develop = 8926b3f`。
> 方法：JDK17(`ms-17.0.18`) + `backend/` Gradle 8.7 wrapper `clean build`；真实 MySQL8@3307 + Redis7@6380；后端运行态（`:18080`）HTTP API 实测 + 直查 DB 核对账目/状态机；**并发用例为 QA 自写**（Python 线程 barrier + 2/10 并发）。
> 约束：未改业务源码、未动飞书、未 push。QA 自建干净链路数据（`QA_P2_*`/`QA2_*` 系列），未复用工程师冒烟数据出结论。

---

## 0. 结论

**后端核心（T01–T10 + 三重校验并发）：IS_PASS = true（第 2 轮维持）。**
**整体（含第 3/4 轮前端+联调）：IS_PASS = false。** 第 4 轮（`613d65f`+`e59ac70`）：#28/#29/#31 关闭 ✅，但新增 **#33【P1】枚举字符串↔数值契约断裂**（合同登记/供应商编辑等枚举表单 UI 4000）与 **#32【P2】待审批定标无 UI 提交入口**；QA 2 轮复验上限已到，按规程收口为 Known Issues 移交（见 §0.4/§0.5/§7）。

- 三重校验并发正确性（合规重点）与主链路状态机/账目守恒**全部通过**（第 1、2 轮双重复验）。
- 第 1 轮 P1×2/P2×2 于 `8926b3f` 全部修复并复验关闭（见 §0.3）；P3 项 #25 已按定稿口径（剔除式）实现并验证，#26/#27 已改进。
- 第 3 轮（P2 前端 13 页 + 双端联调，`5f7ed3b`）：构建/渲染/菜单全部通过，但 **UI 写操作被雪花 ID 精度丢失系统性阻断**（见 §0.4）。

### 0.1 第 1 轮（本轮，HEAD `a86bd3e`）验证汇总

| # | 复验点 | 结果 |
|---|---|---|
| 1 | 构建 `clean build`（JDK17，含 `:test`） | ✅ BUILD SUCCESSFUL，**42 例全绿**（P1 29 + P2 13：`OrderTripleCheckTest` 5 + `PurchaseApplyServiceTest` 8），0 fail/error |
| 2 | ★三重校验并发正确性 | ✅ 见 §2（2 线程 × 2 场景 + 10 线程压力：**恰一方成功**、守恒差 0、无死锁、无负值、拒单不落库） |
| 3 | ★主链路端到端 | ✅ 见 §3（申请→两级审批→询价准入→比价→截标→定标拆分→合同→下单→分次到货→台账→考核→履约调整，状态机全程正确） |
| 4 | 快照一致性（五类明细行） | ✅ 见 §4（申请/报价/定标/订单快照与 `unit_conversion` 当前生效一致；**未来生效版本不被取**） |
| 5 | 延后项台账核查（D1–D8/B1–B2） | ✅ 见 §5（预留口子全部在位；`used_amount` 零业务写入；入库不写 Outbox；`SETTLED/PAID` 预留未启用） |
| 6 | P1 回归 + P2 菜单 | ✅ P1 主数据/上传 4000 与 1002 降级/导出/畸形请求 400/4000/401/403/404 全过；菜单 12 根 + **26 子**（P1 14 + **P2 新增 12**：401-405/601-602/801-804/1001）⚠️ 与任务书"13 项"差 1，以 seed 为准待主理人确认口径 |
| 7 | 边界 | ✅ 无 token 401、无权限 403(2004)、未知路径 404(4040)、畸形 JSON 400/4000（P3-9 未回退） |

### 0.2 分级问题摘要

- **P0**：无。
- **P1**：第 1 轮 2 项（#21/#22）→ **第 2 轮全部关闭**。
- **P2**：第 1 轮 2 项（#23/#24）→ **第 2 轮全部关闭**。
- **P3**：#25（混合发布口径）→ 已按 PM 定稿"剔除式"实现并验证关闭；#26（导出格式）/ #27（口径说明+定标单头）→ `8926b3f` 改进并验证关闭。无新增开放项。

### 0.3 第 2 轮：复验修复 `8926b3f`（QA #21–#27）

> 方法：`clean build`(JDK17) **51 例全绿**（42 + 新增 `QuotationImportTest` 5 / `ContractPerSupplierAmountTest` 3 / `AwardMainSupplierTest` 1）；后端运行态重建独立链路（`QA2_*`：供应商 A/B 有效、C 黑名单、SKU_SMOKE1 BOX→PCS rate=12），逐修复点针对性实测 + 直查 DB 对账。菜单 12 根 + **27 子（P2 二级 13 项**，含补种 805 `/order/assess` 服务考核）。

| # | 复验内容 | 结果 |
|---|---|---|
| #21 | 模板 SKU/供应商 ID 改**文本写出**（回读 `'2102245024849907713'` 精确）；**模板原样只填单价/数量→导入 success=1**（第 1 轮必败场景现已通过）；导入侧文本/数值双兼容 | ✅ 关闭 |
| #22 | 合同金额按**该供应商**定标明细 Σ：A(95×144=13680)/B(92×144=13248) 两家各自登记成功并 EFFECTIVE；**误填总额 25632 → 4000「Σ 该供应商定标明细金额：13680.00」**；Σ 份额合同 26928 = 定标总额守恒 | ✅ 关闭 |
| #23 | 批次失效按供应商：A(95)/B(92) 两家有效报价**共存**（均 invalid=0）；比价 min=91/max=95/n=2（多次重导入后旧批次正确置 invalid=1） | ✅ 关闭 |
| #24 | 错误行导入**整批拒绝不落库**（success=0、B 名下 0 行）+ **errorSheetBase64**（xlsx `PK` 魔数，行号/原因）随 JSON 返回；随后合法重导成功 | ✅ 关闭 |
| #25 | 剔除式发布：publish [A,B,C(黑名单)] → code=0；`inquiry_supplier` A/B **invited=1**、C **invited=0 + 快照留痕**「已列入黑名单、无有效资质」；全部不可用才 4000「无可用供应商」 | ✅ 关闭（按 PM 定稿口径） |
| #26 | 申请导出 **OOXML**（`PK` 魔数、双 Sheet「申请单头/明细」、含快照列），可被 openpyxl 解析 | ✅ 关闭 |
| #27 | 定标单头 = **金额最大主供应商**（A 95 放第 1 位、B 92 末位 → head=A=max≠末位，排除"取最后一家"回归）；单价=基本单位口径已补说明 | ✅ 关闭 |

- **并发回归（第 2 轮重跑）**：10 线程齐发 → `{0:1, 3000:9}` 恰 1 成功；余量 12/0；B 合同账目守恒差 **0.00000**。✅ 未回退。
- 菜单补种：805 `/order/assess`（order:assess:read/write）已授权，P2 二级 **13 项**与任务书口径一致。✅
- 诊断说明：本轮脚本曾出现 3 处"FAIL"，经查均为 QA 脚本自身问题（SQL 结果首行截断的假阴性、断言沿用第 1 轮数值、测试文件自带重复行），**非产品缺陷**，已逐项用全量输出复核纠正。

### 0.4 第 3 轮：P2 前端 + 双端联调（HEAD `5f7ed3b`）—— IS_PASS=false

> 方法：前端 `npm run build`（无 chunk 告警，最大 386.76 kB）；后端 `clean build` **51 例全绿**（无回归）；`vite dev :5173`（`VITE_API_PROXY_TARGET=http://127.0.0.1:18080`，端口受后端 CORS 白名单约束——5188 等非白名单端口会被 CORS 403 拦截登录）；agent-browser(Chromium) 浏览器 E2E + DB 对账。证据截图 `docs/qa-evidence/fe2-*.png`。

**通过项**：

| # | 复验点 | 结果 |
|---|---|---|
| 1 | 前端独立构建 | ✅ `npm run build` 绿、**0 chunk 告警**（最大 386.76 kB，与 P1 收口一致） |
| 2 | 后端回归 | ✅ `clean build` **51 例全绿**（无回归） |
| 3 | 侧栏 P2 二级 13 项可达 + 菜单图标 | ✅ 登录后 13 页逐页打开，全部渲染非空白，图标正常 |
| 4 | 逐页 page error / console error | ✅ **13/13 页 = 0**（两轮清扫均 0） |
| 5 | 报价模板下载（#21 回归） | ✅ 模板 SKU ID 已为**文本**（`'2102245024849907713'` 精确回读） |
| 6 | 申请导出 OOXML（#26 回归，后端直连正确 ID） | ✅ `application/vnd...spreadsheetml.sheet`，PK 魔数，双 Sheet |
| 7 | 审批驳回必填校验（前端） | ✅ 空意见提交被 `inputValidator` 拦截，对话框保持打开 |
| 8 | P1 页面渲染回归（单位/产品库/供应商/资质/预算台账入口） | ✅ 抽样正常；`v-permission` 写按钮两态稳定、越权 403 未回退 |

**阻断缺陷（P1 级，详见 §7 #28/#29）**：UI 写操作系统性失败——

| UI 操作 | 输入（真实 ID） | 后端实际收到 | 结果 |
|---|---|---|---|
| 新建采购申请（SKU ID 手填） | `2102245024849907713` | `2102245024849907700` | 4000「SKU 无效或已停用」**不落库**（fe2-03） |
| 新建询价（来源申请 ID） | `2102435312942956546` | `2102435312942956500` | 3003「仅已审批的申请可发起询价」（实为 ID 不存在的兜底文案）（fe2-04） |
| 报价导入（询价 ID） | `2102447593638522882` | `2102447593638523000` | 4040「询价单不存在：2102447593638523000」（fe2-05） |
| 合同登记（供应商/定标 ID） | `2102245025428721667` | `2102245025428721700` | 4040「供应商不存在：2102245025428721700」（fe2-08/10） |
| 审批同意/驳回（任务列表 row.id） | `2102448678293925889` | `2102448678293926000` | **幽灵成功**：toast「已同意/已驳回」，DB 任务仍 PENDING、无 approval_record（fe2-06/07） |

- 根因隔离（API 对照）：同一询价请求，正确 ID → `code=0` 创建成功；损坏 ID → `3003`。**后端无缺陷，纯前端 ID 传输损坏**。
- 影响面：JS `JSON.parse`（axios 默认）对 >2^53 整数舍入（雪花 ID 处间距 256，仅 1/256 概率幸存）+ 9 个视图文件 13 处 `Number(id)` 二次损坏 → **P1/P2 所有依赖雪花 ID 的 UI 写操作近乎全废**；P1 轮次未暴露是因为 P1 E2E 的写路径走 API 或小自增 ID（unit 表 id=1,2,3）。
- 因 #28 阻塞，本轮**无法通过 UI 验证**：剔除式发布提示、错误 Sheet 下载、拆分定标 UI、订单超额 toast、trace 弹窗、分次入库 UI、履约调整 UI（对应后端行为均已在后端两轮验证）。

### 0.6 第 5 轮（补充抽验，非正式复验轮次）：#32/#33 修复确认（backend `5b41625` + frontend `72381d6`）

> 背景：第 4 轮按 2 轮上限收口后，工程师主动修复 Known Issues 并请求归档确认；QA 做**轻量抽验**（不重启完整复验流程）。双端构建：后端 53 例全绿、前端 build 0 告警。证据 `fe3-01`~`fe3-03`。

| # | 抽验内容 | 结果 |
|---|---|---|
| #33② | 供应商枚举数值化（`@JsonValue`） | ✅ 详情接口 `status/coopStatus/isBlacklist/source` 均为 **int**（与分页一致）；UI「详情回填→保存」→「保存成功」，DB 一致（fe3-01；第 4 轮同场景 4000） |
| #33① | 合同类型数值下拉 | ✅ UI 合同登记穿透到业务层：误填 100 → **4000「合同金额必须等于 Σ 该供应商定标明细金额：28800.00」**（toast 原样，fe3-02）；改 28800 → 「合同已登记」，DB `contract_type=0` |
| #32 | 定标「提交审批」按钮 | ✅ PENDING_APPROVAL 定标行按钮出现 → UI 提交 → 确认弹窗 → 任务创建（fe3-03） |
| P3 分页 | el-pagination 弃用警告 | ⚠️ **部分残留**，详见 #34 |

34. **【前端·P3，2 处】分页遗留**（第 5 轮抽验新增）：
    - a) `PurchaseRequest.vue:24` 漏改 `:current-page`（应为 `v-model:current-page`）——该组件挂**活跃路由 `/purchase`（登录默认页）**，Deprecated 警告仍现（工程师所称"14 处"实为 15 处）；
    - b) `usePagination.js:23` `total.value = data.total || 0`：#28 Long→String 后 `total` 为字符串（`"total":"1"`）→ el-pagination `total` prop 期望 Number → **每个分页页 console 均有 1 条 Vue type warn**。修：`total.value = Number(data.total) || 0`。
    均 P3 非阻断；#34 不改变本报告收口判定。

### 0.5 第 4 轮：UI 收口复验 #28–#31 修复（backend `613d65f` + frontend `e59ac70`）—— IS_PASS=false（#28/#29 关闭，新增 #33/#32）

> 方法：双端独立构建（后端 `clean build` **53 例全绿**、前端 build 386.76 kB 无告警）；vite dev `:5173` + 后端 `:18080` 浏览器 E2E + DB 对账。证据 `fe2-11` ~ `fe2-22`。

**已关闭**：

| # | 复验内容 | 结果 |
|---|---|---|
| #28 | Long→String 全局序列化 + 前端去 `Number(id)` | ✅ **关闭**。响应 ID 全字符串（`"id":"2102…667"`）；UI 全链路实测：①新建申请（SKU 手填 …713）保存成功、DB `sku_id=2102245024849907713` 精确、快照 BOX 2→base 24/rate 12 正确（fe2-12 前置）；②UI 提交→审批同意→DB 真实生效（apply 2→3）；③UI 下单成功（DD-202609-000011，额度 1056→0、余量 24→12 扣减正确）；④trace 弹窗全链路 ID 精确；⑤询价创建/发布/报价导入/比价/截标 UI 全通 |
| #29 | 审批幽灵成功 | ✅ **关闭**。`POST /approvals/tasks/999999/approve` → **4040「审批任务不存在」**；UI 同意/驳回后 DB 任务/单据状态真实变化、approval_record 落库 |
| #31 | mojibake | ✅ 关闭。供应商 B/C、申请标题中文显示正常 |

**新增问题（第 2 轮上限内如实记录为 Known Issues，QA 不修源码）**：

33. **【P1·前后端契约】枚举字符串 ↔ 数值 Integer 断裂 → 枚举类表单 UI 全部 4000**。第 3 轮报告的"日期选择器自动化受限"结论**订正**为：合同登记/供应商编辑失败与日期无关，根因是枚举值序列化不对称。
    - 前端 STRING_ENUMS（`constants/enums.js`）以**字符串枚举名**为 option value（`itemType: [{value:'MATERIAL'},…]`），EnumSelect 提交 `'MATERIAL'`；后端 `ContractSaveReqVO.contractType` 为 `Integer` → Jackson 非数字字符串反序列化失败 → **4000「请求体格式非法」**（UI 合同登记 100% 不可用，fe2-16）。
    - 更广暴露面：**详情接口枚举序列化为名字符串**（`GET /suppliers/{id}` → `status:"PENDING"`/`coopStatus:"NORMAL"`/`isBlacklist:"NO"`/`source:"PLATFORM"`；而分页接口同字段是数值 `"source":0`，**同字段两接口序列化不一致**）→ 「详情回填→保存」的表单（供应商编辑）整批回传字符串枚举 → 4000（fe2-22；DB 未变）。API 二分定位：`PUT` 最小 payload（无枚举字段）code=0 成功、带 `status:"PENDING"` 即失败。
    - 建议：后端统一枚举序列化策略（详情/分页一致；VO 接受枚举名或前端映射为数值）。修复面：至少 ContractIndex（contractType）、SupplierList（回填保存）、及所有 `EnumSelect ↔ Integer` 表单。
32. **【P2·前端】待审批定标无「提交审批」按钮** — `AwardIndex.vue:17` 条件 `['DRAFT','REJECTED'].includes(row.status)`，但后端状态为 `PENDING_APPROVAL(0)` → 待审批定标 UI 无提交入口（API 可提交），新建后流程在 UI 断链。建议改为 `['PENDING_APPROVAL','REJECTED']`。
- **P3**：13 页 console 均有 ElementPlus `[ElPagination] Deprecated usages` 警告（非 error，建议升级分页用法）。
- **#30 未独立复验**：#28 修复后已无法自然构造"坏 blob 导出"场景（ID 不再损坏），以工程师自测 + 代码审查（request.js 拦截器 JSON 嗅探）为准。

**本轮通过的完整链路**（UI 或 API 补位推进）：申请（UI 保存快照正确→UI 提交→UI 审批→APPROVED）→ 询价（UI 创建/发布剔除式快照 A invited=1 / 黑名单 C invited=0+留痕，confirm 文案明确）→ 报价（UI 导入成功 1 行；**错误批整批拒绝 toast「整批未落库」且 DB 无新增**）→ 比价（min/max/avg）→ 截标 → 定标（UI 拆 2 家，黑名单供应商被后端准入校验拦截=有效验证；改有效供应商后 APPROVED）→ 合同（**误填总额 25632 → 4000「Σ 该供应商定标明细金额：12672.00」原样返回**；按份额 12672/12960 登记均 EFFECTIVE，Σ=25632 守恒）→ 订单（UI 超量「余量 24/本单 36」、超额「可用 1056/本单 2112」均 4000 且不落库；正常下单成功扣减正确）→ 到货分次入库（7+5 → PARTIAL→RECEIVED）→ trace → 台账 → 履约调整（50<5% 免审直接生效；500>5% 走审批）。

---

## 1. 验证 1：干净重编（含测试）

- `export JAVA_HOME=ms-17.0.18`；`backend/` 下 `./gradlew clean build` → **BUILD SUCCESSFUL**（12s，8 tasks）。
- 测试 **42 例全绿**，分布：P1 29（GlobalExceptionHandler 7 / FileController 3 / IntegrationWorker 2 / SupplierAdmission 6 / SupplierPage 2 / SupplierSkuBindCycle 1 / SysUserSave 8）+ P2 13（`OrderTripleCheckTest` 5 / `PurchaseApplyServiceTest` 8），failures=0 errors=0。
- 运行态：`supply-chain-backend.jar --server.port=18080`（3307/6380 覆盖）启动 9.5s，MinIO 未启降级 WARN（符合 P1 设计）。

## 2. ★验证 2：三重校验并发正确性（合规重点，独立复现）

### 2.1 串行三重校验（HTTP API + DB 对账）

| 用例 | 结果 |
|---|---|
| O1 合法下单（1 BOX@88 → base 12，amount 1056） | ✅ code=0；`contract.available 25632→24576`；`apply_item 12/12`；订单 `budget_occupied=1056`；申请→PARTIAL_ORDER(5) |
| O2 超余量（2 BOX=base 24 > 余 12） | ✅ 拒绝 code=3000「超出申请余量」；**DB 前后逐字节不变**（available 24576、12/12） |
| O3 超额度（1 BOX@999999 → 11,999,988 > 1056） | ✅ 拒绝 code=3000「超出合同可用额度」；**不落库不扣减** |
| O4 变更（1 BOX→0.5 BOX，差额回冲 528） | ✅ available 24576→25104；余量 6/18；`order_change` before/after 快照留痕、status=生效(1) |
| O5 取消 | ✅ available 全额回冲 25632；余量 0/24；订单 CANCELLED(4)；`order_change change_type=4` 留痕；申请状态回退 |
| 守恒 | ✅ `amount − available == Σ(非取消订单金额)`：CON 25632−25104=528 与订单行 Σ 完全一致（浮点差 **0.00000**） |

### 2.2 并发（QA 自写线程 race，barrier 同步齐发）

| 场景 | 配置 | 结果 |
|---|---|---|
| **RACE-B 额度竞争** | 合同 1056；两线程各 1 BOX@88（各 1056；余量 24 可容两单） | ✅ **恰 1 成功 1 拒**（0.02s/0.05s 返回，无死锁）；`available=0`；余量 12/12；守恒成立 |
| **RACE-A2 余量竞争** | 申请余量 24；两线程各 2 BOX（各 base 24） | ✅ **恰 1 成功 1 拒**；item 24/0；available 25632→23472（=−2160）；守恒差 0 |
| **RACE-10 压力** | 余量 1 BOX（base 12）；10 线程齐发各 1 BOX | ✅ **1 成功 9 拒**（`{0:1, 3000:9}`）；item 12/0；**守恒差 0.00000**；无负值、无死锁、无请求超时 |

> 锁实现核对：`OrderServiceImpl.createOrder` Redis 锁 `contract→apply` 固定顺序 + `selectForUpdate` 行锁（contract 先、apply_item 主键排序 IN）+ `deductRemain/deductAvailable` 条件 UPDATE（version 兜底重试 1 次）——与设计 §4.2 一致；实测无交叉死锁（全部请求 <100ms 返回）。

## 3. ★验证 3：主链路端到端（QA 自建数据 `QA_P2_*`）

| 环节 | 结果 |
|---|---|
| 采购申请 | ✅ 明细选有效 SKU；落快照三字段（2 BOX→base 24、rate=12）；金额=Σ（数量×预估单价） |
| 提交+两级审批 | ✅ `submit` → `BUDGET_PENDING`(软校验)→`PURCHASE_PENDING`(2)；`approval_task bizType=PURCHASE_APPLY` 落 `DEPT_HEAD`；`/approvals/tasks/{id}/approve` → 任务 APPROVED、`current_node→PURCHASE_DEPT`（两级节点语义保留）、申请→APPROVED(3)。本地桩"即审即过"=前端/接口触发即过，符合 §3 预期 |
| 询价+准入拦截 | ✅ 仅 APPROVED 申请可建；发布逐家 `getAdmission`：**无资质供应商被拒**（code=4000「供应商准入校验未通过…无有效资质」），仅合格者发布→PUBLISHED(1)；`inquiry_supplier.admission_snapshot` 落 JSON（qualified/validity/reasons）审计可回溯；增补范围同样过准入+落快照 |
| 报价模板/导入 | ⚠️ 模板可下载（OOXML `PK`）；**导入存在 #21 P1 缺陷（模板数值 ID 精度丢失→必败）**；文本格式单元格导入成功（价格≤0/未知 SKU 行级错误收集正常，见 #24） |
| 比价 | ✅ `comparison` 按 SKU 汇总 min/max/avg + 历史价；仅统计 `invalid=0` 有效批次 |
| 截标 | ✅ PUBLISHED→CLOSED(2)，范围锁定 |
| 定标 | ✅ 按 SKU 拆 2 家供应商（12 BOX@88 / 12 BOX@90）；`award_item` 落 qty/qty_in_base_unit/conv_rate_snapshot；AWARD 审批通过→APPROVED(1) |
| 合同 | ⚠️ #22 P1 缺陷（金额按定标单总额）；金额错配→4000「合同金额必须等于 Σ 定标明细金额：25632.00」校验本身生效；审批通过→EFFECTIVE 且 `available_amount=amount` ✅；**终止**→TERMINATED(4)、available 冻结、下单被拒「合同未生效：已终止」✅；**续签**→新合同 `renewed_from_id` 关联、独立 DRAFT ✅；`expiring-warn` 200 ✅ |
| 下单 | ✅ 见 §2.1（含物料/服务拆单：服务申请转单生成 `order_type=1` 订单） |
| 到货/入库 | ✅ 一订单多次到货；到货明细按订单明细展开（qty_expected=余量 12）；部分入库 4→`PARTIAL_STORED`；足额→`STORED`；首单到货订单→`PARTIAL_RECEIVED`、全部足额→`RECEIVED`；**入库口径=基本单位** ✅ |
| 台账/追溯 | ✅ `/arrivals/ledger` 3 条流水可查；`/orders/{id}/trace` 返回 订单↔申请↔合同↔明细 全链路 |
| 服务考核 | ✅ 物料订单考核被拒（3003「仅服务订单可进行服务考核」）；服务订单登记 score=90/deduct=100 落库（P3 结算取数口子 D8） |
| 履约调整 | ✅ 500 元（<5%×25632=1281.6）免审直接**生效(2)**；2000 元（>阈值）→审批中(1)+`FULFILLMENT_ADJUST` 任务→同意→生效(2) |

## 4. 验证 4：快照一致性（五类明细行）

- `unit_conversion` 现存有效版本 BOX→PCS rate=12（effective 2026-09-22）。
- **未来生效版本排除**：临时插入 rate=99、`effective_from=2027-01-01` 行后新建申请 → 快照仍取 **12**（qty_in_base_unit=12、conv_rate_snapshot=12.000000），未取未来版本；清理后复测一致。✅
- 五类行实测：`purchase_apply_item`（2/24/12）✅、`quotation`（2 BOX→qty_in_base 24、purchase_unit BOX）✅、`award_item`（12→144、rate 12）✅、`order_item`（qty_purchase 1、qty_base 12、`conv_snapshot={"purchaseUnit":"BOX","rate":12}`）✅、`arrival_item` 继承订单口径（base 12/6）✅。历史单据不受后续换率变更影响（快照落值）。

## 5. 验证 5：延后项台账核查（`docs/延期需求与占位登记.md`）

| 台账项 | 预留口子核查（代码级） | 结论 |
|---|---|---|
| D1 日常/框架链路 | `purchase_apply.type`（`PurchaseApplyType`，`PurchaseApply.java:25`）；`contract.renewed_from_id`（nullable，续签实测写入） | ✅ 在位（P2b） |
| D2 线下补录 | `type=2` 预留；`contract.award_id` nullable（手填金额合同 CON3 实测通过） | ✅ 在位（P2b） |
| D3 预算硬控制 | `BUDGET_PENDING` 枚举+状态机注释；`budget_status`（实测超额→2 仅提示不拦截）；`purchase_order.budget_occupied`（实测=1056 快照）；`used_amount` **grep 全库 0 处业务写入**（仅读） | ✅ 在位（P3） |
| D5 审批中心 | `ApprovalGateway` 接口 + 4 bizType 全部实测（PURCHASE_APPLY/AWARD/CONTRACT/FULFILLMENT_ADJUST）；`LocalApprovalGateway` 注释明确 P4 换实现；两级节点 `DEPT_HEAD→PURCHASE_DEPT` 保留 | ✅ 在位（P4） |
| D6 外部系统 | `ArrivalServiceImpl` 0 处 Outbox 写入（确认入库不写事件）；`outbox_event` 运行期无新增 | ✅ 在位（P6） |
| D7 供应商 H5 | `supplier_qual` 无改动（P1 闭环复用：实测资质创建→审核→回调→VALID） | ✅ 在位（P7） |
| D8 结算/付款 | `service_assess.deduct_amount` 落库实测（100）；`OrderStatus.SETTLED(3)/PAID(5)` 预留未启用（全库无业务引用） | ✅ 在位（P3） |
| B1 演示胶囊 | 前端项，本轮后端范围外 | — |
| B2 综合评分 | `comparison` 仅多维展示无自动算分 | ✅ 符合 |
| 代码内 `<!-- Dx -->` 标注 | **26 处**（D1×3/D2×4/D3×9/D5×6/D8×4），均就地标注可顺藤摸瓜 | ✅ |

## 6. 验证 6：P1 回归 + 边界

- **准入四态**：VALID（2 家实测）→ qualified=true；NONE（无资质）→ false+「无有效资质」；EXPIRED/黑名单路径由 P1 第 3 轮覆盖且本轮询价发布拦截复用同一 `getAdmission`。✅
- **菜单**：12 根 + 26 子（P1 14 + P2 12）。⚠️ 任务书口径"13 项"，seed 实际 12 项（401-405/601-602/801-804/1001），待主理人确认是否另有一项未种（如"比价"独立入口）。
- **上传**：缺 file 部件 → 400/4000（P3-9 未回退）；MinIO 停 → code=1002 降级 ✅。
- **畸形 JSON** → 400/4000 ✅；**无 token** → 401 ✅；**无权限用户**（`qa_p2_user` 0 perm）→ **403/2004** ✅；未知路径 → 404/4040 ✅。
- **申请导出**：`purchase-requests/{id}/export` → text/csv（含明细+换算率快照列，UTF-8）；规格未限定格式（P1 导出为 OOXML），记 #26（P3 信息项）。
- **P3-20 回归**（`b480eea`）：`POST /api/v1/org/users` → 200/code 0，密码哈希入库 ✅（上阶段遗留已清）。
- **常购清单**：新建/部门列表/bring-in（停用 SKU 拒绝逻辑在 `FrequentPurchaseServiceImpl`，本轮 SKU 有效带入成功、最近价回填 88）✅。

## 7. 问题清单（分级，带证据）

### P0（阻断/数据/安全）
- 无。

### P1（功能缺陷，须修）
28. **【已关闭】前端雪花 ID 精度丢失（P1 系统性）** — 第 4 轮复验：后端 `JacksonConfig` Long→String + 前端移除 `Number(id)` 后，UI 全链路 ID 精确（申请快照/审批生效/下单扣减/trace），✅ **关闭**（详见 §0.5）。
    双重根因：① axios 默认 `JSON.parse` 将 >2^53 的 JSON number 舍入（雪花 ID 量级 2.1e18，间距 256，实测 `2102245025428721667 → 2102245025428721700`，幸存率仅 1/256）；② 9 个视图文件 **13 处 `Number(id)`** 二次转换：`ApplyIndex.vue:205`、`OrderIndex.vue:148,149,157,188`、`ContractIndex.vue:111,112`、`ArrivalIndex.vue:106`、`AdjustIndex.vue:92,93`、`AssessIndex.vue:98`、`InquiryIndex.vue:135`、`AwardIndex.vue:135,144,147`、`QuotationIndex.vue:103`、`FrequentIndex.vue:75`。
    实测复现（浏览器 E2E + DB 对账，见 §0.4 表）：新建申请/询价/报价导入/合同登记/供应商编辑全部 4000/3003/4040 且不落库；**审批同意/驳回幽灵成功**（toast 成功但 DB 未变，被 #29 掩盖）。证据 fe2-03~fe2-10。
    建议（二选一，推荐 A）：**A. 后端全局 Long→String 序列化**（Jackson `ToStringSerializer` 对 Long/long，一次修复全链路，前端去 `Number()` 改字符串直传）；B. 前端引入 json-bigint 解析（侵入大、易漏）。另需同步排查 H5 端。
29. **【已关闭】审批幽灵成功（P1·后端）** — 第 4 轮复验：不存在任务 → 4040「审批任务不存在」，UI 审批 DB 真实生效，✅ **关闭**（详见 §0.5）。
    `ApprovalTaskController.java:59-63（approve）/ 71-79（reject）` 无条件 `R.ok(true)`；`LocalApprovalGateway.callback` 对不存在的 taskId 不报错（幂等语义吞没"不存在"）。复现：`POST /api/v1/approvals/tasks/999999/approve` → `{"code":0,"data":true}`；reject 同。与 #28 叠加后，审批中心 UI **100% 操作显示成功但零生效**。
    建议：callback 前校验任务存在，不存在返回 4040「审批任务不存在」；仅对"已终态的同一决策"保留幂等成功。

21. **【后端·P0 功能】报价模板以数值类型写 19 位雪花 ID → Excel double 精度丢失 → 按模板导入必然失败** — `QuotationServiceImpl.exportTemplate`（SKU/供应商 ID 列写数值单元格）+ 导入侧数值解析。
    复现（独立复现，100% 必现）：下载 `/api/v1/inquiries/{id}/quotation-template` → 模板 SKU 单元格回读 `2102245024849907713 → 2102245024849907712`（差 1）；原样填价上传 `/api/v1/quotations/import?inquiryId=` → `{"success":0,"fail":1,"errors":["第2行：SKU 不属于该询价：2102245024849910000"]}`（解析值进一步浮点舍入）；**将单元格改为文本格式后同文件导入成功**（success=1，快照 qty_in_base=24 正确）。
    影响：R-INQ-02（P0）实际不可用——供应商/采购员按模板填写报价导入**开箱必败**，主链路在报价环节断链（除非手改单元格格式，现实不可行）。
    建议：模板 ID 列以文本格式写入（`DataFormat` "@" + `setCellValue(String)`），导入侧兼容数值/文本（`DataFormatter` 或字符串读取）。
22. **【后端·P0 口径】合同金额校验按定标单**总额**（含其他供应商份额）** — `ContractServiceImpl.java:85-93`（`Σ award_item` 无 `supplier_id` 过滤）。
    复现：定标按 SKU 拆 2 家（GOOD 12×88、GOOD2 12×90，总额 25632）→ 登记 GOOD 合同 `amount=12672`（其真实份额）→ **4000**「合同金额必须等于 Σ 定标明细金额：25632.00」；GOOD2 合同 `12960` 同样被拒，被迫登记 25632 → 两张合同总敞口 51264 ≫ 定标 25632，额度守恒失真且 GOOD 可用额度含 GOOD2 货值（下单越份额"合法"）。
    违反规格 R-AWD-01 AC②「可拆分给多家」+ R-CON-01 AC①「合同金额 = Σ来源定标明细金额」（应按**该供应商**明细 Σ）。
    建议：`awardTotal` 计算增加 `.eq(AwardItem::getSupplierId, req.getSupplierId())`。

### P2（语义口径 / 健壮性，建议修）
23. **报价批次失效为询价全局而非按供应商** — `QuotationServiceImpl.java:196-200`（`.eq("inquiry_id")` 无 `.eq("supplier_id")`）。
    复现：GOOD 报 88（batch -05 有效）→ GOOD2 导入 90（batch -06）→ **GOOD 的 -05 被置 invalid=1**。违反 R-INQ-02 AC⑤「**同一供应商**重复导入生成新批次，旧批次置失效」；后果：多家有效报价无法共存，比价退化为"最后导入的一家"（本轮 comparison 实测仅剩 1 家有效），削弱比价竞争与定标择优。
24. **错误行导入"部分落库"** — 复现：3 行（价格 -5 / 价格 0 / 合法 88）导入 → `{"total":3,"success":1,"fail":2,"errors":[...]}` 且**合法行已入库**（batch -05 落 1 行）。规格 AC④「错误行收集为错误 Sheet…**全部通过才落库**」。现行为"逐行校验、合格行照落"，与 AC 不符（另：错误仅以 JSON 返回，无"错误 Sheet"文件）。建议：整批事务化，存在错误行则整体拒绝。

### P3（口径确认 / 提示）
30. **【前端·P3】导出按钮不校验业务码，错误 JSON 被静默保存为 .xlsx** — 第 4 轮：工程师已改 request.js 拦截器嗅探 JSON 错误体统一 toast；因 #28 修复后无法自然构造坏 blob 场景，**QA 未独立复验**，以工程师自测+代码审查为准。
31. **【已关闭·P3】冒烟数据 mojibake** — 第 4 轮复验：3307 库 4 行已修复（根因 docker exec 缺 `--default-character-set=utf8mb4`），供应商 B/C 与申请标题中文显示正常，✅ **关闭**。
33. **【前端+后端契约·P1，未修复】枚举字符串 ↔ 数值 Integer 断裂 → 枚举类表单 UI 全部 4000**（第 4 轮新增；订正第 3 轮"日期选择器受限"误判）。
    - 证据 ①：`ContractIndex.vue:44` `EnumSelect(enum-key="itemType")` 提交 `'MATERIAL'`，后端 `ContractSaveReqVO.contractType` 为 `Integer` → **4000「请求体格式非法」**，UI 合同登记 100% 不可用（fe2-16；`contractType:0` 数值直发 API 即通过校验到达业务层）。
    - 证据 ②：详情接口枚举序列化为**名字符串**（`GET /suppliers/{id}` → `status:"PENDING"`/`coopStatus:"NORMAL"`/`isBlacklist:"NO"`/`source:"PLATFORM"`），而**分页接口同字段为数值**（`"source":0`，两接口不一致）→「详情回填→保存」的供应商编辑 UI 回传字符串枚举 → **4000，DB 未变**（fe2-22）。API 二分：最小 PUT（无枚举字段）code=0，带 `status:"PENDING"` 即失败。
    - 影响面：所有 `EnumSelect ↔ Integer` 表单与"详情回填→保存"页面。修复建议：后端统一枚举序列化策略（详情/分页一致；VO 接受枚举名或前端提交前映射回数值），前端 STRING_ENUMS 对数值型后端字段做名→值映射。
32. **【前端·P2，未修复】待审批定标无「提交审批」入口** — `AwardIndex.vue:17` `['DRAFT','REJECTED'].includes(row.status)` 但实际状态 `PENDING_APPROVAL(0)` → 待审批定标 UI 无提交按钮、流程断链（API 可提交，本轮以 API 推进后续链路）。建议条件改为 `['PENDING_APPROVAL','REJECTED']`。
25. **混合发布全有或全无**：publish [合格,不合格] → 整体 4000、`inquiry_supplier` 空（无任何快照）。规格 AC③「qualified=false **拒绝加入范围**」可读作"剔除不合格者、合格者照常入范围"；现行为原子拒绝（更严）。**需 PM 拍板口径**；若维持现状建议错误消息列出被拒供应商及原因（现已含）。
26. **申请导出为 CSV**（text/csv，含快照列）；P1 其他导出为 OOXML，格式不统一。规格未硬性规定，建议统一。
27. **金额/单价口径需文档明确**：报价/定标/订单单价均按**基本单位**计价（`amount = price × qty_in_base_unit`），与模板「报价单位 BOX + 单价(元)」的"按采购单位报价"直觉存在 12 倍误读风险；另拆分定标时定标单头 `supplier_id` 取最后一行供应商（`a86bd3e` 落值逻辑），语义弱。建议在设计文档 §7.2 补一句"单价=基本单位口径"并明确单头供应商字段语义（或置空）。

### 已修复确认（本阶段）
- **P3-20（用户创建 500）**：`b480eea` `SysUserServiceImpl.save` BCrypt 编码 — 实测 `POST /api/v1/org/users` → 200/code 0。✅ 关闭。

## 8. 已验证 / 未验证边界

**已验证（后端，第 1–2 轮）**：`clean build` 42→51 例；三重校验串行 6 用例 + 并发 3 场景（2×2 + 10 线程，全部守恒差 0、无死锁）；主链路 13 环节状态机与 DB 对账；快照一致性 5 类行 + 未来版本排除；D1–D8/B2 代码级在位核查 + 运行时验证；P1 回归（准入/上传/导出/菜单/畸形请求/401/403/404）；#21–#27 修复逐项关闭复验。

**已验证（前端+联调，第 3 轮，`5f7ed3b`）**：`npm run build` 无告警；后端 51 例无回归；侧栏 P2 二级 13 项可达、图标正常；13 页 page/console error=0；模板下载文本 ID 回归通过；导出 OOXML（正确 ID）回归通过；驳回必填前端校验；P1 页面渲染/权限回归；**#28 精度丢失 5 条 UI 路径独立复现 + API 根因隔离 + #29 幽灵成功实锤**。

**未验证（因 #28 阻塞或超范围）**：~~剔除式发布提示、错误 Sheet 下载、拆分定标/订单超额 toast/trace 弹窗/分次入库/履约调整 5% 免审的 UI 路径~~（→ **第 4 轮已全部补验通过**，见 §0.5）；H5 端（不在本轮范围）；`purchase_apply` 导出数据权限部门隔离（沿用 P1 结论）；#30 坏 blob toast（#28 修复后无法自然构造，见 §0.5）。

**第 4 轮（`613d65f`+`e59ac70`）已验证**：#28/#29/#31 逐项关闭（§0.5）；UI 申请→提交→审批→APPROVED；UI 下单超额/超量 4000 不落库 + 正常扣减；UI 分次入库→RECEIVED；trace/台账/履约调整阈值（50 免审生效、500 走审批）；询价剔除式发布 UI + 快照留痕；报价导入成功/错误批整批拒绝；定标提交时黑名单供应商被准入校验拦截；构建 53 例/无告警。

**未验证（第 4 轮遗留 Known Issues）**：#33 修复后的枚举表单回归面（合同登记 UI、供应商编辑 UI、及所有 EnumSelect↔Integer 表单）；#32 修复后的 UI 定标提交路径——均待下一修复轮次（QA 2 轮上限已到，不再复验）。

## 9. 路由建议

- **#29 → Engineer（后端）**：✅ 已修 `613d65f`（4040 + 幂等保持），第 4 轮复验通过 → 关闭。
- **#28 → Engineer（前端为主）**：✅ 已修 `613d65f`+`e59ac70`（Long→String + 去 Number(id)），第 4 轮复验通过 → 关闭。
- **#33 → Engineer（software-engineer-3）+ Architect 裁决契约方向**（本轮新增 P1，未修复）：统一枚举序列化策略；修复面含 ContractIndex/SupplierList 及全部 EnumSelect↔Integer 表单。
- **#32 → Engineer（前端，未修复）**：`AwardIndex.vue:17` 条件改为 `['PENDING_APPROVAL','REJECTED']`。
- **#30/#31 → 低优先随修**；#25 口径已定稿，无动作。
- **环境备忘（非缺陷）**：后端 CORS 白名单仅含 5173/5174 等，vite dev 须用 5173 端口（5188 会被 CORS 403）；本机 8080 被占，后端须 `--server.port=18080`。

- **#21/#22 → ✅ 已修 `8926b3f`，第 2 轮复验通过 → 关闭。**
- **#23/#24 → ✅ 已修 `8926b3f`（同文件 QuotationServiceImpl 一并修复），第 2 轮复验通过 → 关闭。**
- **#25 → ✅ PM 定稿"剔除式"，`8926b3f` 实现并复验通过 → 关闭。**
- **#26/#27 → ✅ `8926b3f` 改进（OOXML 双 Sheet / 单头取金额最大主供应商 + 口径说明），复验通过 → 关闭。**
- 备忘（非阻断、不计开放项）：①`createInquiry` 仅接受 `status=APPROVED` 的申请，申请已 PARTIAL_ORDER/FULL_ORDER（全部转单）后**不可再次询价**（3003）——规格"一个申请可多次询价"未明确覆盖已转单场景，建议 P2b 与 D1 日常链路一并明确；②错误 Sheet 目前以 `errorSheetBase64` 内嵌 JSON 返回（QA 已验证可解码），如需独立文件流端点可后续优化。
- **总判定：后端核心第 1–2 轮 IS_PASS = true（维持）；前端+联调第 3/4 轮 IS_PASS = false —— #28/#29/#31 已关闭，遗留 #33【P1】/#32【P2】为 Known Issues；QA 2 轮上限已到，移交主理人路由。**
