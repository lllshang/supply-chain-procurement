# P4 审批中心真实接入 — QA 独立验证报告

- **验证人**：software-qa-engineer-8（QA 严过关，fresh eyes 独立复现，不复述工程师自报）
- **验证基线**：分支 `develop`，HEAD=`ada01aa`（P4 四提交 7b1a535/3e73804/b1c75e8/ada01aa），设计基线 `ef05ff1`
- **验证日期**：2026-09-25
- **判定基准**：`docs/P4审批中心设计.md`（实现契约）+ `docs/P4审批中心规格设计.md`（AC）+ `docs/审批节点拍板清单.md`（默认值）

---

## 结论：**IS_PASS**（附 2 个非阻断缺陷：#1 P2 / #2 P3，台账清零建议见 §6）

---

## 1. 构建复现（独立复跑）

| 项 | 工程师自报 | QA 独立复跑 | 结论 |
|---|---|---|---|
| 后端 `gradlew clean build`（JDK17 ms-17.0.18） | 209 例绿 | **BUILD SUCCESSFUL，JUnit 汇总 tests=209, failures=0, errors=0, skipped=0**（从 build/test-results XML 独立统计） | ✅ 证实 |
| 前端 `npm run build` | 386.76kB | **✓ built in 3.41s**，element-plus-upper chunk 386.76 kB | ✅ 证实 |

## 2. 种子契约端到端（新建库 scm_qa4 全自动初始化）

MySQL8@3307 新建空库 `scm_qa4`，后端 `--server.port=18080`（Redis 6380）启动后**应用自动初始化**，DB 实测：

| 契约 | 期望 | 实测 |
|---|---|---|
| approval_flow_def | 8 流 | 8 ✅ |
| approval_node_def | 10 节点 | 10 ✅ |
| 审批菜单 1001/1002/1003 | 3 条，1001 语义升级 | 3 条；1001=待办审批 /approval/todo perms=approval:todo,approval:approve,approval:read ✅ |
| contract_type | 3 条（MATERIAL/SERVICE/MIXED） | 3 ✅ |
| sys_role 审批 5 角色 | 5 | 5 ✅ |
| admin 兼任审批角色 | SUPER_ADMIN+5 | sys_user_role user_id=1 共 6 行；登录 JWT roles 含全部 5 审批角色 ✅ |
| 菜单 603 / 802 perms | contract:type:* / receipt:over-receive | 均已带 ✅（DB 实查） |

**p4_migration.sql 幂等**：对已初始化库重复执行 2 次，仅文件头声明过的 ALTER 1060 报错（可忽略口径），种子零重复、计数不变 ✅。

## 3. 运行态冒烟（API 层独立复现，全部真实链路落库验证）

### 3.1 BUDGET 升级完整闭环（工程师冒烟时未走通的路径，本次补齐）
- 造当月预算行（dept=1×subject=1×2026-09 月度行 10 万）→ 申请 20 万提交 → `BUDGET` 任务生成（amount=200000，apply 停 BUDGET_PENDING(1)、budget_status=2、未占用）✅
- 批准 → **used_amount 0→200000（超支占用真生效）+ 申请 1→2（PURCHASE_PENDING）+ 自动补发 PURCHASE_APPLY 两级审批任务** ✅
- 两级审批 N1（部门负责人）→N2（采购部）→ 申请 2→3（APPROVED）✅
- **驳回路径**：另一超预算申请，驳回空意见被 4000 拦（双拦截）→ 带意见驳回 → 申请 1→4（REJECTED）、预算零占用 ✅

### 3.2 DAILY_AUTH
- 日常订单 55 万（>50 万阈值）→ auth_over_limit=1 + DAILY_AUTH 任务（REQUIRES_NEW 独立事务）✅
- **批准** → auth_over_limit 1→0 + 订单 remark 留痕「超授权已确认(任务…确认人admin…)」✅
- **驳回**（工程师未测）→ 复用 cancelOrder 链：订单→CANCELLED(4)、合同 available_amount 回冲（40000→600000）、任务终态 REJECTED ✅ 数据一致

### 3.3 合同审批 + 金额区间节点
- 40 万合同 → 仅 N1 生效；60 万合同 → N1+N2 两级（LEADER 节点金额区间 ≥50 万激活）✅ 拍板清单 Q5 占位默认生效

### 3.4 D15 供货范围三分支全覆盖
| 分支 | 场景 | 实测 |
|---|---|---|
| 有清单（A1） | 合同含 price_item（SKU9001@100） | 价格不符 150→**硬拦截**「合同清单价 100.00，本单 150.00」；SKU 不在清单→**拦截**；精确命中→通过 ✅ |
| 无清单有定标 | — | 单测覆盖（209 例内），运行态以分支①③实证机制（同一第四重校验链）|
| 皆无 | 空白名单合同 | 下单通过=现网行为 ✅；**白名单 PUT `[{skuId}]` 裸数组契约**→GET 回读一致；白名单 [9002] 后 SKU9001→**拦截**「不在合同供货范围（SKU 白名单）」、9002→通过；PUT `[]` 覆盖式清空→恢复现网 ✅ |

### 3.5 超收授权（R3b）双阈值运行态
- **阈值 0（默认）**：超收直接拒「超收阈值为 0（禁止超收）」✅
- **重启切阈值 10**：超收 50%→拒「超过授权阈值 10%」；超收 10% 且持 receipt:over-receive→放行，arrival_item qty_actual=11 落库 ✅

### 3.6 SUPPLIER_QUAL 正向收敛（P1 资质闭环回归）
- 资质提交审批 → SUPPLIER_QUAL 工作流任务 → 批准 → qual status 流转 + reviewed_by 留痕 ✅（正向 handler 生效）

### 3.7 工作台/通知/权限
- /api/v1/menu：审批中心含 1001/1002/1003（perms 与种子一致）✅
- todo（候选人过滤）/done（含 recordId/action/approverName）/search/flows 端点 ✅
- 通知红点：unread-count 21→单条已读 20→read-all 0 ✅
- **权限两态**：低权用户（仅挂无权限角色）对 todo/done/search/flows/approve 全部 2004 无访问权限；admin 全功能 ✅（QA-5 同法）

### 3.8 幂等与一致性
- 节点快照：approval_task 提交即写 approval_node 快照行（node_code/seq/version，DB 实查 N1/N2）✅
- 版本乐观锁：approval_task.version/approval_node.version 运行态可见递增；并发防重审由 WorkflowApprovalGatewayConcurrencyTest 覆盖（209 例内）✅

## 4. 代码对照设计

| 检查项 | 结果 |
|---|---|
| ① ApprovalGateway/TaskSpec/Decision/CallbackHandler 签名零变更 | `git diff ef05ff1..ada01aa` 对四契约文件 **diff 为空** ✅ |
| ② 8 个业务发起点零改动 | 7 个业务 ServiceImpl 的 gateway 调用 diff 为空（第 8 个=BUDGET handler 内补发，属 P4 新增范围）✅ |
| ③ 节点链快照落 approval_node | 运行态 DB 验证 ✅ |
| ④ Caffeine TTL60s+保存即失效 | ApprovalFlowConfigService：expireAfterWrite(60s)+invalidate/invalidateAll 双保险，FlowConfigCacheTest 覆盖 ✅ |
| ⑤ 新代码无裸 hasAnyPerm | approval 控制器全部具名 hasPerm（todo/done/approve/config/detail/nodes）；仅 /search 保留 hasAnyPerm=**注释声明「兼容端点（退役中）」**，属显式豁免 ✅ |
| ⑥ schema.sql ↔ p4_migration.sql 双份一致 | 5 新表列清单逐表脚本比对 **MATCH**；种子（流/节点/菜单/合同类型/角色/授权）三源一致 ✅ |
| ⑦ router L65 死条目 | **证实**：`router/index.js` L52 `{path:'approval',redirect:'/approval/todo'}` 与 L65 `{path:'approval',name:'Approval',component:ModulePlaceholder}` 同 children 数组重复；vue-router 首匹配遮蔽，L65 永不生效（node + vue-router 实验复现：导航 /approval 落 redirect）。**无重复路径 console 告警**（vue-router 仅对重名 name 告警，两处 name 不同）。定级 **P3 清理项**（缺陷 #2），不影响功能 |

## 5. 前端惯例复查

- **Number()/parseInt 对 ID 零残留**：approval 页面仅 FlowConfig.vue L151-152 对**金额**（非 ID）做 Number() 合规转换；TodoList.vue L96 注释明确「ID 全程字符串零 Number() 转换（#28/#1 契约）」✅
- **usePagination**：TodoList/DoneList 均使用 ✅
- **v-permission**：TodoList/DoneList/FlowConfig/ApprovalDetailDrawer 写按钮均挂（2/0/2/2 处；DoneList 为纯只读页合理为 0）✅
- **枚举 map 字符串契约**：`constants/approval.js` 8 bizType 单点配置 + BIZ_TYPE_ROUTES 跳转映射（8 类全映射列表页+bizId 字符串直传）；NODE_STATUS/actionLabel 字符串/int 直传契约清晰 ✅
- **useQueryLocate**：bizId String 直传 + row-class-name 高亮，落 OrderIndex/ContractIndex/SettlementIndex/AdjustIndex/SupplierQual/BudgetLedgerTable 等目标列表页 ✅

## 6. 缺陷清单

### #1（P2）前端 fallbackMenus 缺审批中心子菜单 + 802 perms 缺超收键
- **文件**：`frontend/src/store/user.js:100`（审批中心无 children）、`:81`（到货验收 perms 少 `receipt:over-receive`）
- **现象**：`/api/v1/menu` 接口失败走 fallbackMenus 兜底时，工作台三页无侧边栏入口；超收按钮在兜底态被 v-permission 隐藏
- **复现**：断网/后端 500 时登录，侧边栏「审批中心」无二级菜单
- **建议**：fallbackMenus 补 1001/1002/1003 children + 802 perms 追加 receipt:over-receive（与 data.sql 种子对齐）
- **主链路不受影响**（/menu 正常时菜单正确）

### #2（P3）router/index.js L65 死条目
- **文件**：`frontend/src/router/index.js:65`
- L52 与 L65 同 children 重复 path:'approval'，L65 被 vue-router 首匹配遮蔽为死配置；运行态验证 redirect 生效、无 console 告警；建议 P3 清理删除 L65

### 非缺陷澄清
- BUDGET 批准时无当月预算行 → 仅 log.error 留痕、任务终态已 APPROVED、申请停在 BUDGET_PENDING 无重试入口——**与 P3 设计「行 2 无月度行=需预算升级审批」口径一致**（数据问题触发，非代码缺陷）；工程师冒烟留痕同源
- PUT 白名单收裸数组=**对象数组** `[{skuId,remark}]`（设计 §6.4 List<SkuWhitelistReqVO>），前端 replaceSkuWhitelist 传对象数组一致；传数字数组 400 为正确拒绝

## 7. 台账清零评估（仅评估，台账由主理人更新）

| 台账项 | 结论 | 依据 |
|---|---|---|
| **D5 审批中心真实接入** | ✅ **建议清零** | 真实引擎表驱动（节点快照/候选人/乐观锁/afterCommit 通知）运行态全链验证；工作台 UI 替换占位（todo/done/config 三页+详情抽屉）；PRD §6.16 待办/通知/并发/审计四要素均有落点；「引擎已跑、菜单空壳」临时态消除 |
| **D15 合同供货范围** | ✅ **建议清零** | 方案 A 三分支运行态全覆盖（A1 硬拦截/定标集/白名单兜底），白名单覆盖式维护契约落地，空白名单=现网行为不回溯 |
| **P3c 延后 3 项** | ✅ **建议清零** | 合同类型字典全链（typeId 优先+存量 TINYINT 回填+补充签订独立 CONTRACT 流+CRUD 运行态验证）；超收授权+计重双输入（双阈值运行态验证）；最低价同步开关（默认关=零行为，单测覆盖开启态留痕） |
| **D17 高频补货自动通过** | ⏳ **保留** | 按设计仅键名预留（application.yml 注释、P4 之后），规格 §1.2 Out of Scope，与台账登记一致 |
| 附带核对 | A1 免检判据 | P3c-A1「有清单/无清单」判据未被 P4 合同类型削弱（运行态分支①实证硬拦截不豁免）；台账 A1 ⚠️ 的「P4 需回归确认」已满足 |

## 8. 未覆盖声明

1. **浏览器端到端点击**：本机 playwright-cli 损坏，UI 层以 API+静态契约验证替代；router 死条目用 vue-router 真实语义实验复现。8 bizType 跳转映射为静态契约验证（BIZ_TYPE_ROUTES 8 项全存在、目标路由均在 router 注册），未逐个浏览器点击。
2. **lowest-price-sync-enabled=true 重启运行态**：默认关（拍板 Q12 最保守），开启态 PRICE_SYNC 留痕由单测覆盖，未做运行态重启验证。
3. **会签 ALL / 超时升级 timeout_hours**：设计预留位（v1.x），未验证。
4. **通知多渠道**（sms/wecom/dingtalk）：列预留，未验证。
5. **多实例 Caffeine 缓存一致性**：单实例验证；多实例部署下 60s TTL 内他实例配置可见性依赖 TTL 自然过期（设计已声明不引 Redis 的取舍）。
6. 回归抽样以 **209 例全量绿**（含 P1 资质/P2 主链/P3 结算回归用例）+ SUPPLIER_QUAL/合同/申请/订单运行态抽测为准，未对 P3 结算/对账做逐页手工回归。

## 9. 环境清理声明

- 后端 18080 / 前端 5173 进程已停止 ✅
- 临时库 scm_qa4 已 DROP（含 qa4low 临时账号、全部测试业务数据）✅
- 原库 supply_chain / scm_p3b 等未触碰 ✅

---

*证据均来自 QA 独立复现：build XML 统计、DB 实查、curl API 实测、git diff、vue-router 语义实验。*
