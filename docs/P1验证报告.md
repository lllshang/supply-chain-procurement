# P1 主数据 / 基础配置 · 独立验证报告（后端 + 前端运行态）

> 验证人：QA 工程师 严过关（Edward）
> 验证对象：分支 `develop`，**后端 HEAD `9c44f3a`**（P1 主数据 3 提交 `1edbe2c`→`0cad569`→`45b4032`；修复 `f91fe71` 时区、`c50eb14` P2-2、`629a151` P2-4、`37166ec` P3-5/6/8 收尾、`9c44f3a` 文件上传端点）；**前端 HEAD `766841e`**（后台管理端 4 提交 `6ea58f1`/`f5cc9bc`/`5b505dd`/`766841e`，本地 develop **未 push**）
> 验证方式：**从干净重建 + 对照设计/AC 审代码 + 真实 MySQL8/Redis7（+MinIO）运行时冒烟 + 边界异常路径 + 契约/DDL 一致性核查 + 真实浏览器 E2E（前端运行态）**（共 5 轮 = 后端 4 轮 + 前端运行态 1 轮；后端每轮独立重置 `scm_qa` 干净库复现）
> 验证环境：JDK17（ms-17.0.18）｜Gradle wrapper 8.7｜MySQL 8.0.46（容器 `dzgylxt-p1-mysql`，3307）｜Redis 7.0（`dzgylxt-p1-redis`，6380）｜MinIO（`dzgylxt-p1-minio`，9000，第 4 轮启用；亦可停用验证降级）
> 独立库：新建干净库 `scm_qa`，全程 HTTP 证据 + DB 落库核对；每轮以 `DROP/CREATE DATABASE scm_qa` + 重启后端做**全新独立复现**

---

## 0.3 第 5 轮：前端运行态验证（后台管理端，HEAD `766841e`）

> 本轮验证 P1 **前端（后台管理端）**运行态。方式：静态审路由/权限/API 层 + `npm run build` + **真实浏览器 E2E**（agent-browser 0.19.0 / Chromium，入口 `http://localhost:5173`，登录 admin/admin123）+ 与后端 MySQL8/Redis7/MinIO 联调。未改业务源码、未 push。

- **构建（✅）**：`cd frontend && npm run build` → `✓ built`（2.7s），无错误；仅 Element Plus 整包引入 `chunk > 500 kB` 体积告警（**非缺陷**）。
- **登录 / 默认跳转（✅）**：`http://localhost:5173` → 登录 admin/admin123 成功 → 重定向 `/purchase`。
- **路由静态结构（✅ 但运行态见 #10）**：`router/index.js` 将 14 条 P1 子路由（catalog/category·unit·spec·price-rule·product·import；supplier/category·list·bind·qual；budget/subject·project·import·ledger）挂 `Layout` 下，`/` `redirect:'/purchase'` —— 静态正确。
- **读流程（✅）**：供应商档案列表（2 行）、产品库列表渲染正常；产品**导出**异步流（发起→轮询→下载）实测弹窗「任务状态：成功（`product-export-*.xlsx`）」「导出完成，已开始下载」；预算年度导入向导上传 `budget_ok.xlsx` → 预览「校验通过 / 模板版本 v1 / 年份 2026 / 年度总额 120000 / 明细行 13」。
- **附件上传（✅）**：`FileUpload` 选文件 → `POST /api/v1/files/upload`；MinIO 可用 → 显示 `fileKey` 成功；**MinIO 停用 → toast「服务暂时不可用：文件存储不可用，请稍后重试」（code 1002，非 500）**，降级路径正确。
- **权限渲染（❌ 见 #11）**：`v-permission` 指令已注册且按代码设计工作（perms 非空且缺所需键 → 移除元素）；但 **admin 的 `/auth/me` 仅返回 `*:view`（无任何 `*:write`）**。实测同一页面两种状态：
  | 进入方式 | `/catalog/unit` 结果 |
  |---|---|
  | 整页刷新（perms 尚未加载） | 有「+ 新建单位 / 编辑 / 置无效」 |
  | **SPA 内跳转**（perms 已加载） | 上述写按钮 **全部消失**，操作列空白 |
  即：登录后经界面跳转，管理员在**所有 P1 写操作**（新建/编辑/置无效/绑定/录入资质…）上按钮被移除，呈「刷新可见、跳转即无」的间歇性 → 管理端无法执行增改。
- **后端回归命中（❌ 见 #12）**：供应商**新建**在 UI 提示「保存成功」的同时 toast「系统异常，请稍后重试」；定位为 `GET /api/v1/suppliers/page` 的 500（详见 #12）。

**→ 第 5 轮结论：前端运行态不可放行。** 后端既有结论（第 4 轮 IS_PASS=true）不受影响；但前端存在 **2 项阻断性缺陷**（#10 菜单无 P1 二级→页面不可达；#11 写按钮被 `v-permission` 移除）与 **1 项后端回归缺陷**（#12 `suppliers/page` NPE → 500），须修复后复验。

---

## 0.2 第 4 轮复验刷新（HEAD `9c44f3a`）

> 本轮复验 `37166ec`（P3-8 解绑加固 / P3-5 删死代码 / P3-6 注释）与 `9c44f3a`（`POST /api/v1/files/upload`）。方法：重置干净库 + 启动 MinIO(9000) 做双路径（存储可用/不可用）复现；未改业务源码、未 push。

- **clean build（含 `:test`）**：BUILD SUCCESSFUL；**12 例全过**（`FileControllerTest` 3 + `IntegrationWorkerTest` 2 + `SupplierAdmissionTest` 6 + `SupplierSkuBindCycleTest` 1），0 fail/error/skip。
- **P3-8 解绑加固（✅ 通过）**：同一 (supplier,sku) 连续「绑定→解绑」**4 轮全部 HTTP 200 成功**，无 500/1062；每轮结束后 `active=0, deleted=1`（每对仅保留 1 条已删行）；`GROUP BY supplier,sku HAVING count>2` 为空。
  - 实现核对：`SupplierSkuServiceImpl.unbind()` 加 `@Transactional`，逻辑删除前调用 `SupplierSkuMapper.physicalDeleteDeletedByPair`（`DELETE … WHERE supplier_id=? AND sku_id=? AND deleted=1`）物理清除旧已删行。
  - 健壮性：解绑不存在 id → `code=3001 数据不存在`（非 500）。
  - ⚠ 取舍：物理清理会**永久丢弃上一条已删行**（每对仅留最近 1 条已删），属该方案固有代价，代码注释已说明。
- **`POST /api/v1/files/upload`（✅ 通过）**：
  | 场景 | 结果 |
  |---|---|
  | 未登录 | `HTTP 401 / code=2001` ✅ |
  | 空文件 | `code=4000「上传文件为空」` ✅ |
  | 正常 PNG（MinIO 可用） | `code=0`，`data.fileKey` 非空（`…-_t.png`）、`url` 已填充 ✅ |
  | `file_meta` 落行 | 1 行（`file_key/original_name/size/deleted=0`）✅ |
  | MinIO 不可用 | `HTTP 200 / code=1002「文件存储不可用，请稍后重试」`（**非 500**）✅ |
- **P3-5 死代码（✅ 关闭）**：`BudgetHeaderServiceImpl`、`BudgetLineServiceImpl` 已删除（`impl/budget` 仅余 Import/Query/Project/Subject 4 个）。
- **P3-6 注释笔误（✅ 关闭）**：`p1_alter.sql:2`、`schema.sql:128` 已改为「20 列」。
- **全量回归无回归**：smoke.sh + smoke2.sh 全绿（换算 current=v2 / 限价 / SPU 叶子 / 审批闭环 / 准入四态 / 绑定唯一 / 预算 0..12 13 行 / 权限 401·403·404），T09 导出下载 200（`Microsoft OOXML`），`uk_sup_sku` 索引仍在。
- **本轮新增 P3-9（既有全局问题，非本次引入）**：`GlobalExceptionHandler` 未映射 `MissingServletRequestPartException`（multipart 缺 `file` 字段）与 `HttpMessageNotReadableException`（畸形 JSON 体），二者均落到兜底 → `HTTP 500 / code=1000`。经确认**既有 import 端点同样 500**（`/catalog/spus/import/single`、`/budgets/import/preview`），新上传端点继承该行为；建议补 400/4000 映射。

**→ 第 4 轮结论：IS_PASS = true（维持）。** P3-8/P3-5/P3-6 关闭，上传端点按 AC 通过；仅新增 1 项既有非阻断卫生项（P3-9）。

---

## 0.1 第 3 轮复验刷新（HEAD `629a151`）

> 本轮针对 `c50eb14`（P2-2）、`629a151`（P2-4）做独立复现，并在重置后的干净库上跑全量回归。

- **准入口径四态（运行时，干净库）** —— 全部符合用户/文档口径：
  | 场景 | qualified | qualValidity | reasons |
  |---|---|---|---|
  | 仅 EXPIRING（status=1，未过期） | **true** | EXPIRING | `[]` |
  | 仅 VALID | true | VALID | `[]` |
  | 仅 EXPIRED | **false** | EXPIRED | `["无有效资质"]` |
  | 黑名单（VALID 资质） | false | VALID | `["已列入黑名单"]` |
  | coop_status=2（冻结） | false | VALID | `["合作状态非正常：冻结"]` |
  （`smoke2.sh` C5「EXPIRING-only → qualified=true」独立二次确认。）
- **`supplier_sku` 唯一约束（新建库 `SHOW INDEX`）**：`uk_sup_sku(supplier_id, sku_id, deleted)` 已建成（3 列）；首启日志 **1061 未触发、0 ERROR**。
  - 重复绑定 → `HTTP 200 / code=3002 数据冲突…该供应商与 SKU 已存在有效绑定`（**友好冲突，非 500**）✓
  - **12 并发**绑同一 (supplier,sku) → 体码分布 **{0: 1, 3002: 11}**，库内 `deleted=0` 仅 **1** 行 → 双写竞态被唯一索引兜底 ✓
  - **逻辑删除边界（如实记录，设计取舍，非缺陷）**：`deleted` 为常量 `1`，故每对 (supplier,sku) 至多容 **1 条有效（deleted=0）+ 1 条已删（deleted=1）**。解绑→重绑正常；**第 3 次解绑**（再次把在册行置 `deleted=1`）触发 `ERROR 1062 Duplicate entry '…-1' for key 'supplier_sku.uk_sup_sku'` → `unbind()` 的 `removeById` 未捕获 `DuplicateKeyException` → HTTP 500（已用 SQL 直接复现，见 §3.5）。
- **全量回归无回归**：换算 current=v2、限价拦截、SPU 叶子校验、资质审批闭环、准入 R-X-01、绑定唯一、年度预算 13 行（0..12）、T09 导入（ok/坏数据 3 行级错误）+ 导出（7 行、4053B、`Microsoft OOXML`）、权限 401·403·404 全绿。
- **`clean build`（含 `:test`）**：BUILD SUCCESSFUL；**测试 8 例全过**（`IntegrationWorkerTest` 2 + `SupplierAdmissionTest` 6），由第 1 轮的 2 例增至 8 例。

**→ 第 3 轮结论：IS_PASS = true。** P2-2、P2-4 均按用户口径落地并复验通过；仅遗留 1 项非阻断边界（P3-8）与 2 项历史卫生项（P3-5/P3-6）。

---

## 0. 结论

**后端 IS_PASS: true（第 4 轮复验通过）；前端运行态 IS_PASS: false（第 5 轮）—— P1 整体暂不放行。**

- **后端**：历轮问题（P1-1 时区、P2-2 准入、P2-4 唯一约束、P3-5/6/8）均已修复并复验通过，无回归。

- **第 1 轮**：主体功能全部实测可用，发现 1 个 P1 功能缺陷（单位换算"当前生效版本"查询跨时区恒空，见 §3.1），判定 IS_PASS=false。
- **第 2 轮（复验 `f91fe71`）**：换算端点修复有效、无回归。**缺陷关闭**。
- **第 3 轮（复验 `c50eb14` / `629a151`）**：见 §0.1。P2-2 准入四态、P2-4 唯一约束与并发兜底全部通过；逻辑删除边界记为 P3-8。
- **第 4 轮（复验 `37166ec` / `9c44f3a`）**：见 §0.2。P3-8 解绑加固（4 轮绑解全成功）、通用文件上传端点（401/4000/200+fileKey/1002 降级）、P3-5 删死代码、P3-6 注释修正均通过；`clean build` 12 例绿；全量回归无回归。**IS_PASS 维持 true**。
- **第 5 轮（前端运行态，HEAD `766841e`）**：见 §0.3。构建绿、登录/读流程/上传降级均通过；但发现 **#10 菜单无 P1 二级（页面不可达）**、**#11 `v-permission` 移除全部写按钮**、**#12 `suppliers/page` NPE→500（后端回归）**，**前端运行态 IS_PASS=false**。
- 7 个新实体 + 6 张存量表加字段、16 个 Service 接口↔实现、关键端点均按设计与 AC 通过运行时验证。
- 遗留**非阻断**项：既有全局异常映射缺口（P3-9，multipart 缺字段/畸形 JSON → 500，非本次引入）；测试覆盖可继续加固（P2-3）。

> 说明：P1 缺陷在 **docker-compose 官方编排（MySQL 设 `TZ: Asia/Shanghai`）** 下会被掩盖；但在本次提供的实际运行容器（UTC）下 **稳定复现**。修复后比较基准改为应用时钟，**与 DB 时区解耦**，UTC 场景亦正确。

---

## 1. 验证 1：干净重编（含测试）

| 项 | 结果 |
|---|---|
| 命令 | `cd backend && ./gradlew --no-daemon clean build`（JDK17） |
| 结果 | **BUILD SUCCESSFUL** |
| 产物 | `build/libs/supply-chain-backend.jar`（99.9MB）、`-plain.jar` |
| 测试 | **12 个用例全过**：`FileControllerTest`（3）+ `IntegrationWorkerTest`（2）+ `SupplierAdmissionTest`（6）+ `SupplierSkuBindCycleTest`（1），0 failed / 0 errors / 0 skipped |
| 结论 | 编译与测试全绿；P1 自动化测试由第 1 轮的 **2 例逐步提升至 12 例**（见 §5 P2-3） |

---

## 2. 验证 2：对照设计与 AC 审代码

### 2.1 实体 / DDL（全部通过）

- **7 张新表** 全部落库：`product_category / unit / spec_option / price_rule / supplier_category / budget_subject / budget_project`。
- **6 张存量表加字段 20 列** 全部落库：`spu(+2) / sku(+5) / supplier(+4) / supplier_qual(+4) / supplier_sku(+4) / budget_line(+1)`。
- 全部实体 `extends BaseEntity` + `@TableName`；逻辑删除走全局配置（`mybatis-plus.global-config.db-config.logic-delete-field=deleted`）。
- 9 个新枚举（`CoopStatus/BlacklistFlag/SupplierSource/ValuationType/BindScope/PriceRuleType/PriceRefType/SubjectType/QualStatus`）均实现 `IEnum<Integer>` ✓。
- **唯一索引带 `deleted`**（品类/单位/规格/供应商分类/科目/项目）✓。

### 2.2 分层契约（全部通过）

- **16 个 Service 接口 ↔ 16 个实现，1:1 全配齐**：
  `IProductCategory/Unit/SpecOption/PriceRule/Spu/Sku/UnitConversion/Supplier/SupplierCategory/SupplierQual/SupplierSku/BudgetSubject/BudgetProject/BudgetImport/BudgetQuery/ProductImport` Service。
- VO 包结构 `vo/{catalog,budget,supplier,common}` 与设计 §2 一致。
- Controller 端点与设计 §3 API 清单一致（实现为超集，如新增 `/page`、`/list`，无缺失）。

### 2.3 契约合并核查（通过）

- `BudgetHeaderController` / `BudgetLineController` 两个桩已删除，合并为 `BudgetController`（`@RequestMapping("/api/v1/budgets")`，含 `/import/preview`、`/import`、`/import/tasks/{taskId}`、`/headers`、`/{headerId}/lines`）。
- **无残留引用**（全仓 grep 无命中）。
- **无 `@RequestMapping` 冲突**（31 个类级映射值唯一，无重复）。
- `@PreAuthorize` 门禁覆盖每个 mapping（14 个 P1 Controller 逐一核对，无遗漏）。

---

## 3. 验证 3：运行时冒烟（真实 MySQL8 + Redis7）

### 3.1 ✅（已修复并复验）单位换算"当前生效版本"查询跨时区恒空 — commit `f91fe71`

- **现象**：连续写入两条换算（v1 rate12、v2 rate10）后，
  `GET /api/v1/catalog/unit-conversions/current?skuId=...&fromUnit=QA_U1`
  → `HTTP 200 {"code":0,"message":"成功"}`，**`data` 字段缺失（即 null）**。
- **期望**：返回最大 `version`（=2，rate 10）且 `effective_from <= now` 的记录（设计 §2.2.1、R-X-02）。
- **根因（已用 DB 证据锁定）**：
  - 应用写入 `effective_from` 用 **JVM 本地时间（Asia/Shanghai）** → 落库 `2026-09-22 12:42:50`。
  - Mapper 用 **DB 的 `NOW()`** 比较；容器 MySQL 运行在 **UTC** → `NOW()=04:42:50`。
  - `effective_from <= NOW()` → `12:42:50 <= 04:42:50` = **false** → 无行 → 返回 null。
  - 反证：把比较值改为显式 `'2026-09-22 23:59:59'` 后，SQL 立即返回 v2 行；`SELECT NOW()` 实测 UTC。
- **定位**：`backend/src/main/java/com/dzgylxt/mapper/catalog/UnitConversionMapper.java:18`
  `... AND (effective_from IS NULL OR effective_from <= NOW())`
- **影响**：R-X-02「单位换算快照」跨模块契约（P2 下单取快照）依赖此接口，当前不可用。
- **修复建议**：由 Java 传入基准时间比较（如 `effective_from <= #{now}` 传 `LocalDateTime.now()`），或统一 DB/应用时区；避免"应用写本地时间、DB 用自身 `NOW()`"这类跨时区比较。
- **备注**：本缺陷在 `docker-compose.yml`（MySQL `TZ: Asia/Shanghai`）下不显现，仅在 DB 时区 ≠ 应用时区时暴露；建议仍按上述加固。
- **修复（commit `f91fe71`，本地 develop）**：`selectCurrentEffective` 增加 `@Param("now") LocalDateTime`，SQL 改为 `effective_from <= #{now}`；`UnitConversionServiceImpl.currentEffective` 传入 `LocalDateTime.now()`（与写入同源应用时钟）。
- **第 2 轮复验证据（UTC 容器下）**：
  - `GET .../unit-conversions/current?skuId=…&fromUnit=QA_U1` → **`data` 返回 v2（rate=10，effectiveFrom=2026-09-22T13:15:02）** ✔
  - 语义保持：再写入一条 **未来生效** v3（rate=99，effectiveFrom=次日）后，current **仍返回 v2**（未来版本被 `<= now` 正确排除）✔
  - 全仓 grep 确认无其它 `NOW()`/`CURRENT_TIMESTAMP` 实时 SQL（仅剩 1 处 Javadoc 文字）✔

### 3.2 ✅ 其余端点冒烟（全部通过，附证据要点）

| # | 场景 | HTTP | 结果要点 |
|---|---|---|---|
| 1 | 单位 创建 / 重复编码 / 列表 | 200 | 创建成功；重复→`3002 单位编码已存在`；种子 PCS/BOX/KG/M 在列 |
| 2 | 品类三级树 创建 L1→L2→L3 / tree | 200 | 层级自增、`tree_path` 自动维护、树结构正确 |
| 3 | SPU 挂**非叶子**品类 | 200 | `3000 SPU 必须挂在三级（叶子）品类节点` ✅ |
| 4 | SPU 挂叶子品类 / 重复编码 | 200 | 创建成功；重复→`3002 SPU 编码已存在` |
| 5 | 最高限价规则（max=100） | 200 | 规则创建成功 |
| 6 | SKU 参考价 120（>100） | 200 | **`3000 价格高于最高限价 100.00`** ✅（限价拦截生效） |
| 7 | SKU 参考价 80 / 条码重复 | 200 | 创建成功；重复→`3002 条码已存在` |
| 8 | 单位换算 写入 v1/v2 + history | 200 | 自动版本号 1→2，历史可追溯 ✅ |
| 9 | 单位换算 rate≤0 | 200 | `4000 换算率必须大于 0` ✅ |
| 10 | 供应商 创建 / 重复信用代码 | 200 | 创建成功；重复→`3002 统一社会信用代码已存在` |
| 11 | **准入 R-X-01**（无资质） | 200 | `qualified=false, qualValidity=NONE, reasons=["无有效资质"]` ✅ |
| 12 | 供应商-SKU 绑定 / 重复绑定 | 200 | 绑定成功；重复→`3002 该供应商与 SKU 已存在有效绑定` |
| 13 | 资质 创建→发起审批(Gateway)→回调 | 200 | review 返回 `taskId`；回调驳回**无原因**→`4000 驳回必须填写原因`；回调通过→`status=1` ✅ |
| 14 | 资质通过后 准入 | 200 | `qualified=true, qualValidity=VALID, reasons=[]` ✅ |
| 15 | 黑名单 / 合作状态停用 后准入 | 200 | `qualified=false`，原因分别为`已列入黑名单`/`合作状态非正常：停用` ✅ |
| 16 | 年度预算 预览 / 导入 | 200 | 1 条 header + **13 条 line（period 0..12）**，hundreds=120000、各月 10000、`used_amount=0` ✅ |
| 17 | 预算导入 错误清单 | 200 | `FAILED`，错误定位到 `行2/科目编码/年度总额`（科目不存在 + 金额为负）✅ |
| 18 | 预算头 / 台账查询 | 200 | header 与 13 行明细一致 ✅ |
| 19 | 预算科目树 / 项目 CRUD | 200 | 通过 |
| 20 | T09 产品 单规格导入 / 错误 Sheet / 导出下载 | 200 | 导入成功；坏数据返回 3 条行级错误；导出下载为合法 `Microsoft OOXML`（3878B）✅ |

### 3.3 ✅ 启动与降级

- 应用启动成功（`Started ... in 16.9s`，端口占用 8080 与无关 nginx 冲突，改用 18080 验证）。
- **MinIO 未启 → 优雅降级**：仅 `WARN ... 已降级为不可用，不影响主流程启动`，**未阻断启动** ✅。
- 空库首启自动建 39 张表 + 种子数据（admin/admin123、单位/品类/供应商分类/预算科目）✅。

### 3.4 ✅ 边界与异常路径

| 项 | 结果 |
|---|---|
| 唯一性校验（同 supplier+sku 仅一条有效） | ✅ 重复绑定被拒 |
| 逻辑删除 + 唯一索引共存（解绑后重建） | ✅ 旧行 `deleted=1`、新行 `deleted=0` 共存，重建成功 |
| `bind_scope` 默认值（不传） | ✅ 落库 `bind_scope=0`（A3 口径） |
| 必填 / NOT NULL（SPU 名空、单位编码空） | ✅ `4000` 参数校验 |
| 树形引用完整性（不存在的父品类/品类） | ✅ `3001 数据不存在` |
| 价格规则字段组合（未知类型、区间 min>max） | ✅ `4000` 拦截 |
| 包装单位引用不存在的单位 | ✅ `3001 单位不存在：NOPE` |
| 权限门禁：无 Token | ✅ `HTTP 401`（`2001 未登录`） |
| 权限门禁：0 权限用户 | ✅ `HTTP 403`（`2004 无访问权限`） |
| 未知路径 | ✅ `HTTP 404`（`4040 请求的资源不存在`） |
| 资质有效期派生 VALID / EXPIRING / EXPIRED | ✅ 均正确派生（见 §4 P2-2 关于 EXPIRING 准入口径） |
| 中文落库 | ✅ `name` HEX 为合法 UTF-8（`QA供应商A`） |
| 启停用联动 | ⚠️ P1 无单据、无跨实体联动实现；设计明确"引用拦截点由 P2 落地"，**P1 不可验证，非缺陷** |

### 3.5 ✅ 第 3 轮新增：`supplier_sku` 唯一约束与逻辑删除边界（干净库复现）

- **索引建成**：新建 `scm_qa` 后 `SHOW INDEX FROM supplier_sku WHERE Key_name='uk_sup_sku'` → 3 列 `supplier_id, sku_id, deleted`（BTREE，Non_unique=0）✓。
- **首启幂等**：`CREATE TABLE` 已内联 `uk_sup_sku`，末尾 `ALTER TABLE … ADD UNIQUE KEY` 未触发 1061；启动日志 0 ERROR ✓。
- **重复绑定（友好冲突）**：`POST /api/v1/supplier-skus`（重复对）→ `HTTP 200 / {"code":3002,"message":"数据冲突，请刷新后重试：该供应商与 SKU 已存在有效绑定"}`（**非 500**）✓。
- **并发兜底**：12 路并发绑同一 (supplier,sku) → 体码分布 `{code=0:1, code=3002:11}`，库内 `deleted=0` 仅 1 行 ✓（`existsBind` + 唯一索引 + `DuplicateKeyException` 兜底有效）。
- **逻辑删除边界（如实记录，设计取舍）**：
  - 步骤：绑定#1（price10）→ 解绑（`deleted=1`）→ 重绑#2（price12）→ 解绑#2。
  - 前两次操作正常；**第 2 次解绑（即第 3 次写操作）返回 HTTP 500**（`traceId` 记于日志，`{"code":1000,"message":"系统异常，请稍后重试"}`）。
  - **SQL 级直接复现根因**：`UPDATE supplier_sku SET deleted=1 WHERE id=<在册行>` → `ERROR 1062 (23000) Duplicate entry '2102272498669449218-2102272499051130881-1' for key 'supplier_sku.uk_sup_sku'`。
  - **机理**：`deleted` 采用常量 `1`，`uk_sup_sku(supplier_id,sku_id,deleted)` 因而**每对 (supplier,sku) 至多容 1 条有效 + 1 条已删**；同对再解绑即与既有 `…-1` 行撞键。
  - **代码定位**：`SupplierSkuServiceImpl.unbind()` 调用 `removeById(id)` **未**包 `try/catch`（`doBind()` 的 `save()` 已捕获并转 3002；解绑路径未同步加固）。
  - **定性**：第 3 轮按 team-lead 事前拍板，此为 `deleted=常量1` 索引方案的**已知设计取舍**，记为 **P3-8**；**第 4 轮已由 `37166ec` 加固并复验通过（见 §3.6 / §0.2）→ 关闭**。

### 3.6 ✅ 第 4 轮新增：P3-8 解绑加固 + 通用文件上传端点（干净库 + MinIO 双路径）

**P3-8 解绑加固（`37166ec`）**
- 同一 (supplier,sku) 连续「绑定→解绑」**4 轮全部 HTTP 200**，无 500/1062；每轮结束 `SUM(deleted=0)=0, SUM(deleted=1)=1`；`GROUP BY supplier_id,sku_id HAVING COUNT(*)>2` 为空。
- 实现核对：`unbind()` 加 `@Transactional`，逻辑删除前 `baseMapper.physicalDeleteDeletedByPair(supplierId, skuId)`（`DELETE … WHERE … AND deleted=1`）物理清除旧已删行。
- 健壮性：解绑不存在 id → `code=3001 数据不存在`（非 500）。
- 固有代价：物理清理会永久丢弃上一条已删行（每对仅留最近 1 条已删），代码注释已说明。

**`POST /api/v1/files/upload`（`9c44f3a`）**

| 场景 | 证据 | 判定 |
|---|---|---|
| 未登录 | `HTTP 401 / code=2001` | ✅ |
| 空文件 | `code=4000「上传文件为空」` | ✅ |
| 正常 PNG（MinIO 启） | `code=0`，`fileKey=2102281309740208128-_t.png`、`url=http://127.0.0.1:9000/supply-chain/…` | ✅ |
| `file_meta` 落行 | 1 行（`file_key/original_name=_t.png/size=70/deleted=0`） | ✅ |
| MinIO 停 | `HTTP 200 / code=1002「文件存储不可用，请稍后重试」`（**非 500**） | ✅ |
| 缺 `file` 字段 | `HTTP 500 / code=1000`（兜底） | ⚠ 见 P3-9（既有全局缺口） |

---

## 4. 验证 4：DDL 落库与一致性核查（通过）

- `backend/src/main/resources/db/schema.sql` 含 **全部 7 张新表 + 6 张表幂等 ALTER**，空库首启成功建表。
- **三份 SQL 无结构漂移**（重点复核历史"双份 SQL 漂移"问题）：对 7 张新表按列名集合做归一化比对，
  `backend/schema.sql` ≡ `scripts/sql/schema.sql` ≡ `scripts/sql/p1_schema.sql` **列集合完全一致**；`p1_alter.sql` 覆盖全部 20 个加列。
- 索引口径一致：`uk_code(code,deleted)`、`uk_name_value(spec_name,spec_value,deleted)`、**`uk_sup_sku(supplier_id,sku_id,deleted)`** 齐备。
- **`uk_sup_sku` 三源一致性（第 3 轮复核）**：`backend/schema.sql`（内联 L224 + `ALTER` L674）≡ `scripts/sql/p1_alter.sql`（`p1_add_index` 过程 + `CALL` L101）≡ `scripts/sql/schema.sql`（`p1_add_index` + `CALL` L214），**列集合完全一致、无漂移**。

---

## 5. 问题清单（分级）

### P0（阻断/数据/安全）
- 无。

### P1（功能缺陷，须修）
1. **[已关闭]** 单位换算"当前生效版本"查询恒返回空 — `UnitConversionMapper.java:18`（`effective_from <= NOW()` 跨时区比较）。
   首轮复现：写换算记录后 `GET /api/v1/catalog/unit-conversions/current` → `data` 为 null。
   期望：返回最大 version 且 `effective_from<=now` 的记录。
   **修复 `f91fe71` 后复验通过（见 §3.1 第 2 轮证据）。**
10. **【前端·阻断】P1 二级菜单未种子化 → P1 页面无法从界面进入** — `backend/src/main/resources/db/data.sql` 的 `sys_menu` 仅种子 **12 条一级**（`parent_id=0`），`sys_role_menu` 亦仅关联这 12 条，**无任何 P1 二级 children**。
    现象：侧边栏仅 12 个一级菜单、**无二级分组**；点「商品库管理」→ `/catalog`（父路径无匹配子路由）→ **整页空白**；14 条 P1 子路由均无 UI 入口。
    证据：`GET /api/v1/menu` 返回 12 条 `parentId=0` 且**无 `children`**；前端 `fetchMenus()` 因后端返回非空而**不再回退** `fallbackMenus`（`fallbackMenus` 自带 P1 二级 201..206 / 301..304 / 501..504）。
    修复建议：`data.sql` 补种 P1 二级菜单并关联 `sys_role_menu`；或临时改为「后端菜单为空时才回退兜底」。
11. **【前端·阻断】`v-permission` 在 SPA 导航后移除全部写按钮（admin 无 `*:write`）** — `frontend/src/directive/permission.js` 仅在 `mounted` **一次性**读取 `store.userInfo?.perms`（非响应式、无 `updated`）；而 `/auth/me` 对 `SUPER_ADMIN` 仅返回 **12 个 `*:view`**（无 `*:write`）。
    现象（实测同一 `/catalog/unit`）：整页刷新 → 有「新建单位/编辑/置无效」；**SPA 内跳转 → 写按钮全部消失**（两态见 §0.3）。登录后经界面跳转，管理员在**所有 P1 写操作**上按钮被移除。
    修复建议（择一或组合）：① 后端为 `SUPER_ADMIN` 返回通配写权限（或前端按角色放行）；② 指令补 `updated` 钩子、响应式读取 `perms`；③ 统一 `data.sql` 角色权限与前端写权限键口径。
12. **【后端·回归】`GET /api/v1/suppliers/page` 在供应商分类全为 NULL 时 NPE → 500** — `service/impl/catalog/SupplierServiceImpl.java:122` `Map<Long,String> nameMap = categoryIds.isEmpty() ? Map.of() : …`；`line 134` `vo.setCategoryName(nameMap.get(s.getSupplierCategoryId()))` → 全部 `supplier_category_id` 为 NULL 时 `Map.of()` 不可含 null 键，`get(null)` 抛 NPE。
    复现（干净库）：`UPDATE supplier SET supplier_category_id=NULL` → `HTTP 500 / code=1000`，堆栈 `java.lang.NullPointerException at …SupplierServiceImpl.pageSupplier(SupplierServiceImpl.java:134)`；新增 1 个带分类供应商后即恢复 `HTTP 200 / code=0`。UI 现象：供应商「新建」同时 toast「保存成功」+「系统异常，请稍后重试」。
    修复建议：`nameMap.get(...)` 前判空 / 改用可含 null 键的 `HashMap`。

### P2（语义口径 / 健壮性，建议修）
2. **[已关闭]** 准入判定排除 `EXPIRING` 资质 — 经 PM/Architect 拍板"以文档为准（未过期=VALID+EXPIRING）"，工程师 `c50eb14` 修复 `SupplierServiceImpl.getAdmission()`：`hasUnexpiredQual |= (validity==VALID || validity==EXPIRING)`。
   **第 3 轮复验通过**：仅 EXPIRING → `qualified=true`；仅 EXPIRED → `false`；仅 VALID → `true`（见 §0.1 / §3.2 / `smoke2.sh` C5）。**关闭。**
3. **P1 自动化测试覆盖（持续改善）**：第 1 轮仅 2 例 → 第 3 轮 8 例 → **第 4 轮 12 例**（新增 `SupplierSkuBindCycleTest` 1 + `FileControllerTest` 3）。仍建议后续补：单位换算、限价拦截、审批闭环、预算 0..12 台账、`uk_sup_sku` 真库并发等切片测试。
4. **[已关闭]** `supplier_sku` 缺 DB 唯一索引 — 工程师 `629a151` 补齐 `uk_sup_sku(supplier_id,sku_id,deleted)` 并对重复绑定返回友好冲突 3002。
   **第 3 轮复验通过**：索引已建、重复绑定非 500、12 并发仅 1 有效行（见 §3.5）。**关闭**。

### P3（卫生 / 文档 / 健壮性）
5. **[已关闭]** 死代码 — 工程师 `37166ec` 删除 `BudgetHeaderServiceImpl`、`BudgetLineServiceImpl`；第 4 轮核对 `impl/budget` 仅余 Import/Query/Project/Subject 4 个。**关闭。**
6. **[已关闭]** 文档"21 列"笔误 — `37166ec` 已改为「20 列」（`p1_alter.sql:2`、`schema.sql:128`）。**关闭。**
7. （信息）运行环境：容器实际映射 **3307/6380**，与工程默认配置 3306/6379 不一致（默认配置与 `docker-compose.yml` 一致，非代码缺陷）；验证时以环境变量覆盖指向 3307/6380。MinIO 未启不影响启动（已验证）。
8. **[已关闭]** `supplier_sku` 逻辑删除边界 500（原设计取舍）— 工程师 `37166ec` 加固：`unbind()` 加 `@Transactional`，逻辑删除前 `physicalDeleteDeletedByPair` 物理清除旧 `deleted=1` 行。
   **第 4 轮复验通过**：同对连续「绑→解」**4 轮全部 200**，每对仅留 1 条已删行；解绑不存在 id→`3001`（见 §0.2）。**关闭**（注意：物理清理会丢弃上一条已删历史行，属该方案固有代价）。
9. **全局异常映射缺口（既有问题，非本次引入，非阻断）**：`GlobalExceptionHandler` 未映射 `MissingServletRequestPartException`（multipart 缺 `file` 字段）与 `HttpMessageNotReadableException`（畸形 JSON 体），二者落到兜底 → `HTTP 500 / code=1000`。
   已确认**既有端点同样命中**（`/catalog/spus/import/single`、`/budgets/import/preview` 缺 file → 500；`POST /suppliers` 畸形 JSON → 500），新上传端点继承该行为。
   建议：补 `@ExceptionHandler({MissingServletRequestPartException.class, HttpMessageNotReadableException.class, MultipartException.class})` → `400/4000`。

---

## 6. 关键证据索引

- 构建：`BUILD SUCCESSFUL`；测试 12 例（`FileControllerTest` 3 + `IntegrationWorkerTest` 2 + `SupplierAdmissionTest` 6 + `SupplierSkuBindCycleTest` 1），failures/errors=0。
- 建表：`SHOW TABLES`（39 张，含 7 张新表）；`information_schema.COLUMNS` 核对 20 个新列；`SHOW INDEX FROM supplier_sku` → `uk_sup_sku`（3 列）。
- 时区缺陷（第 1 轮）：`SELECT NOW()` → UTC；`effective_from` → Shanghai；显式时间戳查询即命中 v2。
- **第 3 轮准入四态**：EXPIRING→`qualified=true`；VALID→`true`；EXPIRED→`false,["无有效资质"]`；黑名单→`false,["已列入黑名单"]`；coop_status=2→`false,["合作状态非正常：冻结"]`。
- **第 3 轮唯一约束**：重复绑定→`code=3002 …已存在有效绑定`；12 并发→体码 `{0:1,3002:11}`、有效行=1；边界→`ERROR 1062 Duplicate entry '…-1' for key 'supplier_sku.uk_sup_sku'`（SQL 直接复现）。
- **第 4 轮 P3-8**：同对连续「绑→解」4 轮全 `HTTP 200`；每轮 `SUM(deleted=0)=0, SUM(deleted=1)=1`；`GROUP BY … HAVING count>2` 空；解绑不存在 id→`3001`。
- **第 4 轮上传端点**：未登录→`401/2001`；空文件→`4000`；PNG（MinIO 启）→`code=0`，`data.fileKey=2102281309740208128-_t.png`、`url` 非空、`file_meta` 落 1 行；MinIO 停→`code=1002`；缺 `file` 字段→`500/1000`（既有全局缺口）。
- 审批闭环：`review` 返回 taskId；`callback?approved=true` → 资质 `status=1, validity=VALID`；驳回无原因→`4000`。
- 预算：`budget_line` 13 行（period 0..12，used_amount=0）；坏数据→`FAILED` 行级错误（科目不存在/金额为负）。
- T09 导入导出：单规格导入 ok / 坏数据 3 行级错误；导出下载 `HTTP 200`、`Microsoft OOXML`（magic `PK..`）。
- 权限：无 Token→401，0 权限用户→403，未知路径→404；非 multipart 缺查询参数→`400/4000`（处理器正常）。
- MinIO 降级：启动日志 `WARN … 已降级为不可用，不影响主流程启动`。
- **第 5 轮前端（HEAD `766841e`）**：`npm run build` → `✓ built`；登录 admin → `/purchase`；`GET /api/v1/menu` 返 **12 条一级、0 children**；`GET /api/v1/auth/me` perms = **12×`*:view`（0 写权限）**。
  - #10：点「商品库管理」→ `/catalog` **空白页**（截图 `fe_home.png` / `fe_catalog_click.png`）。
  - #11：`/catalog/unit` 整页刷新有「新建单位/编辑/置无效」（`fe_unit_full.png`）↔ **SPA 跳转后全部消失**（`fe_unit_spa2.png`）。
  - #12：`GET /api/v1/suppliers/page`（全分类 NULL）→ `HTTP 500 / code=1000`，堆栈 `NPE at SupplierServiceImpl.java:134`；恢复 1 个带分类供应商 → `200 / 0`。
  - 前端通过项：产品导出异步流「任务成功 + 已开始下载」（`fe_export_ok.png`）；预算导入预览「v1 / 2026 / 120000 / 13 行」；上传 MinIO 停 → toast `1002`。

---

## 7. 路由建议

- **P1-1（单位换算当前生效）→ ✅ 已修复 `f91fe71`，第 2 轮通过。**
- **P2-2（EXPIRING 准入口径）→ ✅ 已修复 `c50eb14`，第 3 轮通过 → 关闭。**
- **P2-4（`supplier_sku` 唯一约束）→ ✅ 已修复 `629a151`，第 3 轮通过 → 关闭。**
- **P3-8（逻辑删除边界）→ ✅ 已加固 `37166ec`，第 4 轮通过 → 关闭。**
- **P3-5（死代码）/ P3-6（文档笔误）→ ✅ `37166ec` 已修，第 4 轮通过 → 关闭。**
- **文件上传端点（`9c44f3a`）→ ✅ 按 AC 通过**（401/4000/200+fileKey/1002 降级/元数据落行）。
- **P3-9（全局异常映射缺口：multipart 缺字段 / 畸形 JSON → 500）→ 既有问题，非本次引入，非阻断**；建议补 `GlobalExceptionHandler` 映射为 400/4000。
- **P2-3（测试覆盖）→ 已由 2 例 → 8 例 → 12 例**，建议后续继续补齐其余 AC 切片测试。
- **#12（`suppliers/page` NPE→500）→ 后端功能缺陷 → 路由 Engineer**（`SupplierServiceImpl.java:134` 空分类判空），修复后回 QA 复验。
- **#10（菜单无 P1 二级 → 页面不可达）/ #11（`v-permission` 移除写按钮）→ 前端阻断缺陷 → 路由 Engineer（+PM 对齐权限口径）**，修复后回 QA 复验。
- **总判定：后端 IS_PASS = true（第 4 轮）；前端运行态 IS_PASS = false（第 5 轮）→ P1 整体暂不放行**，待 #10 / #11 / #12 修复后复验。
