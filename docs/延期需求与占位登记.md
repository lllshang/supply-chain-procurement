# 延期需求与占位登记表

> **目的**：所有因阶段排期或外部依赖（统一认证/审批中心/外部系统）未接入而**延后实现**的需求、以及**临时方案/占位**，统一登记于此。每项注明：完整需求出处、当前临时态、**已预留的口子**、计划落地阶段。
> **铁律**：①任何阶段收尾时必须核对本表，存在未清零项不得标记"整体完成"；②临时方案只允许"换实现"，不允许改契约（接口/状态机/字段一旦按预留实现，后续接入不得返工）。
> 维护人：主理人（齐活林）。登记日期：2026-09-22。

## A. 流程/能力延后（按计划阶段落地，均已确认非删减）

| # | 延后项 | 完整需求（出处） | 当前临时态 | 已预留的口子 | 计划阶段 | 状态 |
|---|---|---|---|---|---|---|
| **D1** | **日常/框架采购链路**（免比价；强制校验合同+预算+授权；超预算/超授权/脱离合同价格范围**升级审批**） | 需求评审裁决2（三链路之一，P0） | **P2b 已实现（2026-09-24）**：DAILY 发询价 3003 拒绝；申请审批后关联合同下单（L743 硬规则：订单必须挂履行中合同）；运行态冒烟过 | 实体 `type` 字段预留；P2 的状态机/校验组件直接复用 | **P2b** | ✅ 已清零（2026-09-24 QA 第3轮 IS_PASS=true）
| **D2** | **线下补录链路**（补录定标与合同后下单，不单设定标审批） | 需求评审裁决2（三链路之一） | **P2b 已实现（2026-09-24）**：手填合同（awardId=null）→审批→无申请来源下单（applyId=null，L747 分流：跳申请头闸门/余量闸，合同额度闸兜底）；运行态冒烟过（合同 5000→4000 扣减对账） | 同上（`type` 预留） | **P2b** | ✅ 已清零（2026-09-24 QA 第3轮 IS_PASS=true）
| **D3** | **预算硬控制**：占用/释放/核销 + 超预算**自动拦截** + 升级审批 | 需求评审 P0"预算校验硬控制（部门+科目），超预算拦截并升级审批"＝进度表行 37–38 | P2 仅**软校验**（读 `budget_line` 余额、超限置 `budget_status=2` **仅提示不拦截**）；`used_amount` 不写 | 状态机预留 `BUDGET_PENDING` 节点 + 升级审批分支；`budget_status`/`budget_occupied` 快照字段；软校验读数逻辑 P3 直接升级为"校验+占用+拦截" | **P3** | ⏳ **用户已确认延后，勿漏** |
| **D4** | **真实统一认证（SSO）接入** | 进度表行 4 登录/权限体系 | `LocalAuthProvider` 本地账号体系 | `AuthProvider` 接口 | **P5** | ⏳ |
| **D5** | **真实审批中心对接**（推送/回调/幂等/重试/留痕；行 30–36 审批单据中心完整 UI） | 进度表行 6 + 行 30–36 | `LocalApprovalGateway` 本地落库即审即过 | `ApprovalGateway` 接口 + 4 个 bizType（PURCHASE_APPLY/AWARD/CONTRACT/FULFILLMENT_ADJUST）+ **两级状态机**（P4 换实现业务代码不动） | **P4** | ⏳ |
| **D6** | **外部系统对接**（二消系统：产品同步/入库单对接） | 进度表行 5 | `NoopAdapter` 仅打日志 | `Adapter` 接口 + `OutboxEvent`/`SyncReconcile` + `IntegrationWorker` 定时投递 | **P6** | ⏳ |
| **D7** | **供应商 H5**（门户/资质 H5 提交/业务查询） | 进度表行 13（H5 提交部分）/ 48–49 | 未实现（后台资质管理 P1 已就绪） | `supplier_qual` 模型+审核闭环已就绪，H5 直接复用 | **P7** | ⏳ |
| **D8** | **结算与付款**（物料/服务/阶段/尾款结算、付款登记台账）、成本管理 | 进度表行 16–17 / 37–44 | **P3 已落地**（占用/核销/对账）；报表中心拆出→**D12**；**前端事务屏已补**（结算管理/付款登记页，2026-09-24 B1/B2） | `service_assess.deduct_amount` 取数已接 | **P3** | ✅ 已清零（2026-09-24 QA 第3轮 IS_PASS=true）
| **D9** | **无申请来源询价（独立寻源）**：`inquiry` 补来源类型+`source_reason` 必填；**线下定标登记入口**（不补建询价单直接落 award）；**⚠️ 接缝缺口（CP-11）**：无申请来源的定标**预算占用锚点未定义** | 审计 R9（PRD BR-07 L1082、§6.6.1 L682、原型已有入口）+ P2↔P3 接缝清单 CP-11 | **P2b 已实现（2026-09-24，CP-11 收口方案A）**：inquiry+source_type/source_reason；线下定标登记（部门×科目必填）；**提交即占预算（锚点=award，QA2-02 跳过分支退役）**；冒烟对账 budget_line used=1000 落科目行 | P3 设计 §0R.9 影响面已列 | **P2b 首批** | ✅ 已清零（2026-09-24 QA 第3轮 IS_PASS=true）
| **D10** | **预付款结算与自动抵扣**：结算类型补"预付款"（payment_stage）、从订单发起→审批→实付→尾款结算自动抵扣（prepayment_deduction）、累计不超订单有效金额 | 审计 R4（PRD §6.10.1 L873、PAY-07 L919） | **P3b 已实现（2026-09-24）**：PREPAYMENT 类型+paymentStage；订单发起→SETTLEMENT 审批→实付→尾款自动抵扣（deduction 审批时回填）；**累计上限含在途双保险**（P1-1：cap committed 口径 + 审批兜底再校验）；payable 口径修正（P2-R2-1：直接取 amount，防双重扣减） | P3 设计 §0R.4 影响面已列 | **P3b** | ✅ 已清零（2026-09-24 QA 第3轮轻量复验 IS_PASS=true） |
| **D11** | **订单状态机收敛**：去 SETTLED/PAID（改派生字段，结算/付款为独立单据状态）；PaymentStatus 补"部分付款"、去 REJECTED（驳回在结算） | 审计 R5（PRD §8 L1114、L918） | **P3b 已实现（2026-09-24）**：订单 SETTLED/PAID 零回写（grep 实证）+ 派生 settleProgress/paidProgress（封顶 1.0）；**PB-01 口径B**：PARTIAL 删除，部分付款=结算维度派生 paidProgress（未付款/部分付款/已付清），PaymentStatus 收敛 UNPAID/PAID | 单据状态独立模型已就绪 | **P3b** | ✅ 已清零（2026-09-24 QA 第3轮轻量复验 IS_PASS=true） |
| **D12** | **报表中心**（采购统计/预算执行/成本/履约/同步报表，维度指标见 PRD §6.12） | PRD §6.12 完整需求（**解除此前"真阻塞"误判**） | 未实现 | 台账数据模型已可支撑取数 | **排期待用户定** | ❓ |
| **D13** | **🟡 字段级范围补齐 12 项**（S4 报价税率/运费/交期、S8 合同类型/补充签订/科目、S9 申请用途/需求时间/项目名、S10 到货拒收退货/超收/计重、S11 服务扣款多条明细、S12 标准价选填/箱规等——明细见审计报告 §1-🟡） | 口径审计 🟡 清单（对照 PRD 原文） | 部分超前实现已登记（S1 price_rule/S2 绑定开关）；**S9 预算科目字段已补**（2026-09-24 接缝收尾 A1：申请表单+后端 occupy/再校验全链带科目），剩余用途/需求时间/项目名未实现 | 审计报告 §3 逐项 ALTER 建议；**📄 正式 Spec 已备**：`docs/P3c字段补齐规格设计.md`（2026-09-24，P3c 主批 6 项 A1–A6 含裁决三条；P4 延后 3 项明确不在批内） | **P3c 主批 6 项 / P4 延后 3 项** | 📄 Spec 备好，**实现排期待用户拍板**（当前用户指令：P3b 收口后先 B6/B7/B8） |

## B. 占位/临时（功能未实现，仅 UI 或桩）

| # | 项 | 现状 | 口子 | 落地时机 | 状态 |
|---|---|---|---|---|---|
| **B1** | 顶栏「演示数据」「供应商H5示例」胶囊 + 折叠按钮 | **纯 UI 无 handler**（代码注明"样式占位，功能未实现"）——用户口径：仅保留按钮 | 前端组件位已留 | 功能明确后实现 | ⏳ 按用户口径保留占位 |
| **B2** | 综合评分法（系统自动计算综合分） | 未做。**已核实原始清单与需求评审均无此要求**（grep 0 命中）；人工定标权完整保留 | 比价分析页展示多维报价对比 | 若业务确认需要 → 排 P2b/P3 | ❓ 待业务确认是否需要 |
| **B3** | 「一个申请可多次询价」是否覆盖 **FULL_ORDER（已全部转单）**申请 | 现状：FULL_ORDER 后 createInquiry → 3003（QA 第 2 轮备忘） | 无口子问题，纯业务口径待定 | **P2b**（与 D1 三链路口径一起定） | ❓ 待口径 |
| **B4** | 错误 Sheet 独立文件流下载端点 | 现状：`errorSheetBase64` 内嵌 JSON 返回（QA 认可可用，前端解码下载） | 可选优化 | 后续按需 | ⏳ 可选 |
| **B5** | 枚举类型化清扫剩余：unit/category/spec/price_rule/qual 等实体+VO 的 status 仍为 Integer | 现状：**序列化路径一致**（均数值，无断裂、无功能影响）；PR-2 已完成 Spu/Sku/Supplier/合同域的枚举类型化（#33 裁决 PR-1+PR-2） | 剩余清单已登记（工程师提交说明） | **PR-3 单独一批** | ⏳ 技术债 |
| **B6** | **P2b-13（QA 第3轮残余 P3）**：无锚订单（D2 手填合同）减额变更后 `budget_occupied` 落负值（−1056 实测）——无资金风险（该字段对无锚订单不参与闸门），字段语义失真、报表可能出负数 | P2b-QA 第3轮运行态实测 | 修复建议：`newOccupied` 加 `max(0,…)` 下界保护（锚点订单不受影响） | 一行修复，随下批预算相关改动顺带 | ✅ 已清零（2026-09-24：`newOccupied` 加 `max(0,…)` 下界 + 无锚减额单测，123 例全绿） |
| **B7** | **P2b-12（部分改善）**：预算生命周期"释放→再占用"仍 mock `budgetOccupyService`——sum 口径类缺陷 mock 测不出（P2b-9 即教训）；**真实 DB 集成测试**建议登记为后续专项 | P2b-QA 第2/3轮实证 | 本轮由 QA 运行态实测弥补 | 集成测试专项（Testcontainers 或专用库） | ⏳ 技术债 |
| **B8** | **P2b-11（被 revert 连带回滚）**：`updateUsedWithRetry` 区分"余额守卫失败"（BIZ_ERROR）与"version 冲突"（3002）的语义修复，随方案 A revert 一并回滚 | engineer-3 `6e0f82e` 含此修复，revert 后丢失 | 核实：修复实际在 engineer-4 `2ad7253` 已落地、revert 方案 A 时反而恢复（本表"丢失"记录过时）；本轮补 2 例错误分类单测（守卫失败→BIZ_ERROR / 真冲突→3002） | ✅ 已清零（2026-09-24 核实+单测验证） |
| **B9** | **P3b-QA 留白**：结算单/预付款无"作废/取消"端点——PENDING 单驳回后仅能 PUT 改额重提，误建的预付款承诺盘子（P1-1 committed 口径含在途）无法释放，会持续占应结盘子 | P3b 修复批实测：controller 域 grep cancel/void/DELETE 零命中（工程师+主理人双确认） | 需设计：PENDING 单作废端点 + 占用冲正语义（与定标作废/合同引用守卫同模式） | 后续结算/预算相关批次顺带 | ⏳ 技术债 |

## C. 已清零（备查）

- ~~P3-9 畸形请求 400/4000~~ ✅（`8ed4a24`）｜~~P3-19 前端分包~~ ✅（`6451be7`）｜~~P3-20 用户创建密码哈希~~ ✅（`b480eea`）｜~~前端样式对齐原型 5 项~~ ✅（`00f8948`~`09031d8`）

## 阶段收尾核对清单（每次用）

- [ ] 本表 A/B 区逐项核对：哪些在本阶段应落地未落地？
- [ ] 新产生的延后/占位是否已登记？
- [ ] 临时方案的**契约**是否与最终需求一致（只换实现不改契约）？
- [ ] 向用户汇报时必须附本表状态。
